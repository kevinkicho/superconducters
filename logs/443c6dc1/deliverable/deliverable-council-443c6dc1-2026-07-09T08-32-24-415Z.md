# Council: directive answer

_4 drafters across 0/0 rounds — directive: "study superconducting materials (with online study if necessary) then please com…"_

> Generated by ollama_swarm preset: **council** · run 443c6dc1-3550-4a21-b682-c4c210d09e21 · 2026-07-09T08:32:24.415Z

## Contents

- [Directive](#directive)
- [Answer to directive](#answer-to-directive)
- [Per-agent positions (latest)](#per-agent-positions-latest)
- [Round 0 — final drafts (full text)](#round-0-final-drafts-full-text)
- [Round 1 — independent first drafts (peer-hidden)](#round-1-independent-first-drafts-peer-hidden)
- [Next actions](#next-actions)

## Directive

study superconducting materials (with online study if necessary) then please come up with chemistry and physics to discover and manufacture room temperature superconducting compounds

## Answer to directive

Now I have a complete picture. Let me produce the merged action plan.
```json
[
  {
    "description": "Consolidate duplicate function definitions in scripts/predict_tc.py: remove 3+ redundant definitions of predict_tc, train_model, average_valence, average_debye, mcmillan_tc, allen_dynes_tc, main; remove the module-level 'TRAINING_DATA = load_database()' call that crashes on import (references non-existent 'superconductor_data.json'); remove duplicate DATABASE definition (A15 vs hydrides) keeping only the hydride database; fix load_database() to point to data/superconductor_database.json",
    "expectedFiles": ["scripts/predict_tc.py"]
  },
  {
    "description": "Fix streamlit_dashboard.py: remove references to tab9/tab10/tab11 (lines ~316-334) since only 8 tabs are created in st.tabs(); remove orphaned 'elif tab == \"Plant Layout\"' and 'elif tab == \"VR Tour\"' blocks where 'tab' is undefined; fix display_candidate_materials() to use data/superconductor_database.json instead of non-existent superconductor.db",
    "expectedFiles": ["streamlit_dashboard.py"]
  },
  {
    "description": "Fix shell injection vulnerability in dft_calculator.py: replace subprocess.run(' '.join(cmd), shell=True, check=True) with subprocess.run(cmd, shell=False, check=True, capture_output=True, text=True) in run_pw, run_ph, run_q2r, run_matdyn, run_lambda functions; remove '>' redirection from command lists and use stdout=subprocess.PIPE instead",
    "expectedFiles": ["dft_calculator.py"]
  },
  {
    "description": "Generate complete requirements.txt by scanning all import statements across the project: add all missing packages (torch, torch_geometric, pymatgen, fastapi, uvicorn, streamlit, stable-baselines3, gym, scikit-learn, GPy, simpy, SALib, networkx, pulp, cryptography, prometheus_client, slowapi, aiohttp, skopt, mp-api, paho-mqtt, reportlab, apscheduler, websockets, plotly, shap, ase, joblib, etc.) with appropriate version constraints",
    "expectedFiles": ["requirements.txt"]
  },
  {
    "description": "Deduplicate and standardize data/superconductor_database.json: remove duplicate entries for the same compound (YH6 appears 8+ times with Tc=200-224K, CaH6 5+ times, Li2MgH16 6+ times, La3Ni2O7 8+ times, Nd0.8Sr0.2NiO2 6+ times); standardize schema to use 'name'/'Tc' keys consistently; remove or flag entries with Tc=0 as non-superconducting; keep most reliable entry per compound (prefer experimental over predicted, peer-reviewed over arXiv)",
    "expectedFiles": ["data/superconductor_database.json"]
  },
  {
    "description": "Fix run_pipeline.py FastAPI endpoints: replace random mock predictions (random.uniform(200,300) and random.uniform(0.5,1.0)) in /predict-tc and /simulate-manufacturing endpoints with actual model inference calls to predict_tc from scripts/predict_tc.py or dft_calculator.py",
    "expectedFiles": ["run_pipeline.py"]
  },
  {
    "description": "Fix scripts/generate_candidates.py code organization: move ConditionalDenoisingDiffusion class, train_conditional_ddpm(), and generate_conditional_ddpm_candidates() definitions before the main() function for readability and to avoid any potential forward-reference confusion",
    "expectedFiles": ["scripts/generate_candidates.py"]
  },
  {
    "description": "Update proposed_chemistry_physics.md with the latest 2024-2026 breakthroughs: add LaSc2H24 (first reproducible room-temperature superconductor, Tc=298K at 260GPa) with Sc-induced gap unification mechanism; add zentropy theory (Penn State, 2025) linking DFT to Tc prediction; add dynamical mid-IR laser stimulation approach for hydrides; add low-pressure ternary hydride families (Li-Mg-H, Na-Ca-H, K-Sc-H) with Tc>300K at <10GPa; add chemical precompression strategies section; add manufacturing roadmap with phased timeline",
    "expectedFiles": ["proposed_chemistry_physics.md"]
  }
]
```

## Per-agent positions (latest)

### Agent 1

Now I have verified all claims against the actual files. Here is my final analysis:

```json
[
  {
    "issue": "scripts/generate_candidates.py defines ConditionalDenoisingDiffusion class, train_conditional_ddpm(), and generate_conditional_ddpm_candidates() AFTER the if __name__ == '__main__': main() block (line ~270). When main() is called with --model_type ddpm, it references these names which have not been defined yet at that point in execution, causing NameError at runtime.",
    "file": "sc

### Agent 2

Now I have verified all claims against the actual files. Here are my findings as Agent 2:

```json
[
  {
    "issue": "scripts/predict_tc.py has a module-level statement `TRAINING_DATA = load_database()` (line ~200) that calls load_database() which tries to open 'superconductor_data.json' — a file that does NOT exist anywhere in the repo. This will raise FileNotFoundError on any import of the module. The hardcoded TRAINING_DATA list defined immediately after is the intended data source, making t

### Agent 3

Now I have verified all claims against the actual files. Here is my analysis as Agent 3:

**Findings I CONFIRM (real issues):**

1. **scripts/predict_tc.py** — Duplicate function definitions confirmed: `predict_tc` (3×), `train_model` (3×), `average_valence` (2×), `average_debye` (2×), `main` (2×), `DATABASE` (2× with different data: A15 vs hydrides). The module-level `TRAINING_DATA = load_database()` at line ~200 will crash on import because `load_database()` is defined later and references non

### Agent 4

Now I have verified all claims against the actual files. Here is my final analysis as Agent 4:

```json
[
  {
    "issue": "scripts/predict_tc.py has 3+ duplicate definitions of predict_tc, train_model, average_valence, average_debye, mcmillan_tc, allen_dynes_tc, and main. Later definitions silently overwrite earlier ones. The module-level statement 'TRAINING_DATA = load_database()' at line ~200 calls load_database() which is defined later in the file and tries to open non-existent 'superconduct

## Round 0 — final drafts (full text)

_(no final-round drafts captured)_

## Round 1 — independent first drafts (peer-hidden)

### Agent 3

Now I have a thorough understanding of the codebase. Here are my findings:

```json
[
  {
    "issue": "run_pipeline.py is a monolithic ~10,000+ line file with multiple duplicate function definitions (sobol_sensitivity_analysis defined 3+ times, reproducibility_check defined multiple times, generate_candidates defined multiple times), multiple if __name__ == '__main__' blocks, and conflicting imports. The main() function references undefined variables 'candidates' and 'predictions' in bayesian_optimization_loop() call, and calls reproducibility_check() which is defined later in the file.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor run_pipeline.py into separate modules (e.g., pipeline_orchestrator.py, active_learning.py, bayesian_opt.py, sensitivity.py, etc.). Remove all duplicate function definitions. Fix the main() function to properly pass candidates/predictions variables or compute them before use."
  },
  {
    "issue": "scripts/predict_tc.py has multiple conflicting definitions of predict_tc, train_model, average_valence, average_debye, and average_atomic_mass functions. DATABASE is defined twice with different data (A15 compounds vs hydrides). TRAINING_DATA is redefined at module level after being used, overwriting the original embedded dataset. The load_database() call at module level references 'superconductor_data.json' which does not exist in the repo.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Consolidate all function definitions to single implementations. Remove duplicate DATABASE definitions. Fix the TRAINING_DATA redefinition issue. Either create superconductor_data.json or change load_database() to use the existing data/superconductor_database.json."
  },
  {
    "issue": "scripts/generate_candidates.py defines ConditionalDenoisingDiffusion class and generate_conditional_ddpm_candidates function AFTER the main() function that uses them, causing NameError at runtime when --model_type ddpm is used.",
    "file": "scripts/generate_candidates.py",
    "severity": "high",
    "suggestion": "Move ConditionalDenoisingDiffusion class and generate_conditional_ddpm_candidates function BEFORE the main() function, or move them to a separate module and import them."
  },
  {
    "issue": "requirements.txt lists only 6 packages (pytest, pytest-cov, numpy, scipy, matplotlib, pandas) but the code imports dozens of additional packages including torch, torch_geometric, pymatgen, fastapi, uvicorn, streamlit, stable-baselines3, gym, sklearn, GPy, simpy, SALib, networkx, pulp, cryptography, prometheus_client, slowapi, aiohttp, skopt, mp-api, paho-mqtt, reportlab, apscheduler, websockets, plotly, etc.",
    "file": "requirements.txt",
    "severity": "high",
    "suggestion": "Generate a complete requirements.txt by scanning all import statements across the project, or use pipreqs/pigar to auto-generate it. Alternatively, create a comprehensive environment.yml for conda."
  },
  {
    "issue": "streamlit_dashboard.py references undefined functions imported from run_pipeline (live_external_validation, what_if_analysis, generate_press_release, generate_monthly_report) but these functions have different signatures or don't exist in run_pipeline. The code also uses variable 'tab' before assignment (elif tab == 'Plant Layout':) and calls display_candidate_materials() at module level which references 'superconductor.db' that doesn't exist.",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Fix all import references to match actual function signatures in run_pipeline.py. Remove or fix the undefined 'tab' variable usage. Either create superconductor.db or change display_candidate_materials() to use the existing data/superconductor_database.json."
  },
  {
    "issue": "app.py has trivial API key validation (only checks if header exists, not its value) and a dummy batch_predict model (20 + 10 * h_count). The in-memory database is lost on restart. No actual Tc prediction model is integrated.",
    "file": "app.py",
    "severity": "medium",
    "suggestion": "Implement proper API key validation using hmac.compare_digest or a secrets manager. Integrate the actual predict_tc model from scripts/predict_tc.py. Add persistent storage (e.g., SQLite or file-based) for candidates and submissions."
  }
]
```

### Agent 1

Now I have a thorough understanding of the project. Here are my findings:

```json
[
  {
    "issue": "run_pipeline.py is a ~10,000+ line monolithic file with dozens of duplicate function definitions (e.g., sobol_sensitivity_analysis defined 4+ times, generate_press_release defined 3+ times, reproducibility_check defined 3+ times, generate_discovery_announcement defined 3+ times, generate_patent_draft defined 3+ times, generate_slide_deck defined 3+ times, generate_research_paper defined 3+ times, generate_discovery_report defined 3+ times, generate_project_closure_report defined 3+ times, generate_weekly_digest defined 3+ times, generate_project_health_report defined 3+ times, generate_technology_transfer_plan defined 3+ times, generate_public_outreach_summary defined 3+ times, generate_comprehensive_final_report defined 3+ times, generate_patent_landscape defined 3+ times, check_regulatory_compliance defined 3+ times, perform_global_sensitivity_analysis defined 3+ times, integrate_arxiv_scraper defined 3+ times, generate_experimental_proposal_report defined 3+ times, validate_model defined 3+ times, retrain_active_learning defined 3+ times, assimilate_experimental_data defined 3+ times, run_closed_loop_simulation defined 3+ times, watch_and_retrain defined 3+ times, comprehensive_validation defined 3+ times, data_assimilation_loop defined 3+ times, run_validation defined 3+ times, integrate_manufacturing_cost defined 3+ times, run_benchmark defined 3+ times, self_optimize_pipeline defined 3+ times, setup_logging defined 3+ times, fetch_and_merge_materials_project_data defined 3+ times, predict_tc_batch defined 3+ times, generate_patent_draft defined 3+ times, automated_document_sync defined 3+ times, supercon_nims_integration defined 3+ times, digital_twin_simulation defined 3+ times, self_healing_mechanism defined 3+ times, decision_support_system defined 3+ times, factory_simulation defined 3+ times, uncertainty_quantification defined 3+ times, pipeline_benchmark defined 3+ times, automated_hypothesis_generation defined 3+ times, patent_paper_generation defined 3+ times, pdf_report_generation defined 3+ times, high_throughput_screening_ternary_hydrides defined 3+ times, generate_synthesis_protocol defined 3+ times, data_quality_checks defined 3+ times, expected_calibration_error defined 3+ times, run_high_throughput_screening defined 3+ times, validate_unified_model defined 3+ times, generate_monthly_report defined 3+ times, generate_funding_proposal defined 3+ times, generate_stakeholder_newsletter defined 3+ times, run_sobol_analysis defined 3+ times, decision_support defined 3+ times, start_health_server defined 3+ times, data_lineage_tracking defined 3+ times, continuous_model_retraining defined 3+ times, multi_objective_optimization defined 3+ times, virtual_lab_simulation defined 3+ times, integrate_arxiv_and_dft defined 3+ times). Later definitions silently overwrite earlier ones, making most of the code dead and unreachable.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor run_pipeline.py into separate modules by domain (e.g., pipeline_orchestration.py, sensitivity_analysis.py, report_generation.py, cloud_lab_integration.py, etc.). Remove all duplicate function definitions, keeping only the latest/most complete version of each function. Use proper Python package structure with __init__.py files."
  },
  {
    "issue": "scripts/predict_tc.py has multiple duplicate function definitions (predict_tc defined 3 times, train_model defined 3 times, average_valence defined 3 times, average_debye defined 3 times, average_atomic_mass defined 3 times, mcmillan_tc defined 3 times, allen_dynes_tc defined 3 times, main defined 3 times). The later definitions overwrite earlier ones, making the earlier code dead. Additionally, the file has a module-level statement 'TRAINING_DATA = load_database()' that calls load_database() at import time, but load_database() expects a path argument and the variable is immediately overwritten by a hardcoded list on the next line.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Consolidate all duplicate function definitions into single canonical versions. Fix the module-level 'TRAINING_DATA = load_database()' call which is immediately overwritten by 'TRAINING_DATA = [...]' hardcoded list. Remove the dead code from earlier definitions."
  },
  {
    "issue": "scripts/generate_candidates.py defines class ConditionalDenoisingDiffusion and functions train_conditional_ddpm, generate_conditional_ddpm_candidates AFTER the main() function that references them. When main() is called with --model_type ddpm, it will raise a NameError because these names are not yet defined at that point in the file.",
    "file": "scripts/generate_candidates.py",
    "severity": "high",
    "suggestion": "Move the ConditionalDenoisingDiffusion class, train_conditional_ddpm(), and generate_conditional_ddpm_candidates() definitions BEFORE the main() function that uses them."
  },
  {
    "issue": "streamlit_dashboard.py references tabs tab9, tab10, tab11 (for Pareto front, uncertainty intervals, status panel) but only 8 tabs are created in the st.tabs() call (Pipeline Status, Top Candidates, Experimental Results, Manufacturing Progress, Experiment Status, Live Monitoring, What-If Analysis, Research Literature). This will cause a KeyError/IndexError at runtime. Additionally, the code has orphaned function calls like collaboration_hub_tab(), plant_layout_tab(), vr_tour_tab() that are referenced via 'elif tab == ...' but 'tab' is never defined in that scope.",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Either add the missing tabs to the st.tabs() call to match the references, or remove the references to tab9/tab10/tab11. Fix the orphaned 'elif tab ==' blocks by either removing them or wrapping them in proper tab context."
  },
  {
    "issue": "dft_calculator.py imports torch_geometric and shap which are not listed in requirements.txt. The file also imports from ase, ase.calculators.espresso, ase.dft.dos which are also missing from requirements.txt. This means the module cannot be imported without installing these missing dependencies first.",
    "file": "dft_calculator.py",
    "severity": "medium",
    "suggestion": "Add torch-geometric, shap, and ase to requirements.txt. Alternatively, wrap the imports in try/except blocks with graceful fallbacks for environments where these packages are not installed."
  },
  {
    "issue": "app.py is a minimal stub with in-memory storage (candidates_db, submissions_db as Python lists) that provides no real integration with the rest of the pipeline. It has a hardcoded API key check (just checks if api_key header is present, not its value) and the batch_predict endpoint uses a dummy formula (counts 'H' characters) rather than calling the actual predict_tc module. The /candidates endpoint returns an empty list since candidates_db is never populated.",
    "file": "app.py",
    "severity": "medium",
    "suggestion": "Integrate app.py with the actual pipeline modules (predict_tc, dft_calculator, etc.) for real predictions. Replace in-memory storage with persistent database (e.g., SQLite or the existing JSON files). Implement proper API key validation against configured keys. Add endpoints to trigger pipeline runs and retrieve results from data/ files."
  }
]
```

### Agent 4

Now I have a thorough understanding of the codebase. Here are my findings:

```json
[
  {
    "issue": "run_pipeline.py imports ~20+ packages not listed in requirements.txt (stable_baselines3, gym, slowapi, cryptography, prometheus_client, pulp, GPy, simpy, SALib, networkx, aiohttp, skopt, mp_api, paho.mqtt, etc.), causing ImportError on any run. The file also defines the same function names multiple times (e.g., generate_press_release, generate_slide_deck, sobol_sensitivity_analysis, reproducibility_check, generate_discovery_announcement, generate_research_paper, generate_candidates, generate_experimental_proposal, generate_discovery_report, generate_materials_physics_report, integrate_real_cloud_lab, study_superconductors, generate_project_closure_report, digital_twin_simulation, multi_objective_optimization, virtual_lab_simulation, continuous_model_retraining, data_lineage_tracking, decision_support, run_sobol_analysis, generate_stakeholder_newsletter, generate_funding_proposal, generate_monthly_report, generate_press_release, validate_unified_model, high_throughput_screening_ternary_hydrides, generate_synthesis_protocol, data_quality_checks, expected_calibration_error, run_high_throughput_screening, start_health_server, integrate_arxiv_and_dft) — each defined 2-3 times with different signatures, so only the last definition survives, breaking callers that expect the earlier signature.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Split run_pipeline.py into separate modules (one per concern: pipeline orchestration, API, simulation, reporting, sensitivity analysis). Remove duplicate function definitions. Add all required packages to requirements.txt or remove unused imports."
  },
  {
    "issue": "scripts/predict_tc.py defines predict_tc() 3 times with different signatures: (1) predict_tc(formula, pressure=0) using Allen-Dynes from database, (2) predict_tc(formula) using random forest, (3) predict_tc(lambda_ep, omega_log, mu_star) using McMillan-Allen-Dynes. Only the last definition survives. Similarly, train_model() is defined 3 times, average_valence() 2 times, average_debye() 2 times. The module-level statement TRAINING_DATA = load_database() calls load_database() which is defined later in the file and references a non-existent file 'superconductor_data.json', causing AttributeError/FileNotFoundError on import.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Consolidate to a single predict_tc() with a unified signature. Remove duplicate definitions. Move TRAINING_DATA initialization inside a function or after load_database() is defined. Fix the file path to point to the actual database at data/superconductor_database.json."
  },
  {
    "issue": "dft_calculator.py imports torch_geometric and shap which are not in requirements.txt. The compute_eph_coupling() function uses subprocess.run with shell=True and string concatenation for commands (e.g., cmd = [PW_BIN, '-in', input_file, '>', output_file] then subprocess.run(' '.join(cmd), shell=True, check=True)), which is a shell injection security risk. The compute_dos_at_fermi() function imports from ase.calculators.espresso and ase.dft.dos which are not in requirements.txt.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Add torch-geometric, shap, and ase to requirements.txt. Replace shell=True with subprocess.run(cmd, shell=False) using proper argument lists. Remove the '>' redirection from the command and use stdout=subprocess.PIPE instead."
  },
  {
    "issue": "scripts/generate_candidates.py defines ConditionalDenoisingDiffusion class and generate_conditional_ddpm_candidates() function AFTER the main() function that references them. When main() is called with --model_type ddpm, it calls ConditionalDenoisingDiffusion() and generate_conditional_ddpm_candidates() which are not yet defined at that point in the file, causing NameError at runtime.",
    "file": "scripts/generate_candidates.py",
    "severity": "high",
    "suggestion": "Move the ConditionalDenoisingDiffusion class and generate_conditional_ddpm_candidates() function BEFORE the main() function, or move them to a separate module and import them at the top of the file."
  },
  {
    "issue": "scripts/query_database.py has a second if __name__ == '__main__': block at the bottom that redefines argument parsing and calls query_supercon_database() or query_room_temperature_candidates(), but the first main() block at the top already handles all argument parsing. The second block will never execute because the first one calls sys.exit() on errors or returns. Additionally, query_external_database() hardcodes API URLs (api.materialsproject.org/materials, supercon.nims.go.jp/api/search) that may not match the actual API endpoints.",
    "file": "scripts/query_database.py",
    "severity": "medium",
    "suggestion": "Remove the duplicate if __name__ == '__main__' block at the bottom. Verify and update the external API URLs to match current documentation. Add proper error handling for network timeouts."
  },
  {
    "issue": "run_pipeline.py defines a FastAPI app with endpoints (/candidates, /predict-tc, /simulate-manufacturing, /health, /metrics) but the __main__ block at the bottom never starts the API server — it calls start_health_server() which starts a separate FastAPI app on port 8000, but the main app with all the endpoints is never served. The /predict-tc endpoint returns a random number instead of using the actual ML model. The /simulate-manufacturing endpoint also returns random values.",
    "file": "run_pipeline.py",
    "severity": "medium",
    "suggestion": "Either serve the main FastAPI app (app) via uvicorn in the __main__ block, or remove the unused API endpoints. Replace random mock predictions with actual model inference calls. Add proper error handling for missing models."
  }
]
```

### Agent 2

Now I have a thorough understanding of the codebase. Here are my findings:

```json
[
  {
    "issue": "streamlit_dashboard.py references undefined tabs (tab9, tab10, tab11) and undefined variable 'tab' in conditional branches, causing runtime crashes",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Remove the `with tab9:`, `with tab10:`, `with tab11:` blocks and the `elif tab == \"Plant Layout\":` / `elif tab == \"VR Tour\":` branches since those tabs are not defined in the `st.tabs()` call. Also remove the duplicate `tabs = st.tabs(...)` at the bottom of the file which redefines tabs and conflicts with the earlier definition in `main()`."
  },
  {
    "issue": "scripts/predict_tc.py has multiple duplicate function definitions (predict_tc, train_model, train_gp_model, average_valence, average_debye, main, generate_candidates, predict_with_uncertainty, allen_dynes_tc, mcmillan_tc) and the module-level TRAINING_DATA is overwritten by a call to load_database() which tries to open a non-existent file 'superconductor_data.json'",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Remove all duplicate function definitions, keep only the last complete version of each. Fix the module-level `TRAINING_DATA = load_database()` call — either remove it or point it to the actual database path `data/superconductor_database.json`. The database uses 'name' and 'Tc' keys but some code expects 'composition' and 'tc' — standardize on one schema."
  },
  {
    "issue": "run_pipeline.py is a ~10,000+ line monolith with 100+ defined functions/classes that are never called (e.g., generate_candidates, run_rl_optimization, generate_experimental_proposal, ingest_experimental_results, run_global_sensitivity_analysis, run_techno_economic_analysis, generate_patent_landscape_report, run_literature_based_discovery, simulate_long_term_stability, run_portfolio_optimization, generate_campaign_plan, run_bayesian_meta_analysis, generate_slide_deck, build_materials_knowledge_graph, unified_tc_prediction, generate_llm_protocol, auto_summarize_papers, benchmark_models, generate_research_paper, run_bayesian_calibration, run_online_learning_drift_detection, optimize_pipeline_hyperparameters, multi_scale_device_simulation, generate_process_design, perform_fmea, generate_patent_draft, check_regulatory_compliance, generate_business_plan, generate_sds_protocol, perform_cost_benefit_analysis, perform_comprehensive_risk_analysis, generate_regulatory_submission, generate_experimental_proposal_report, generate_public_outreach_summary, generate_comprehensive_final_report, generate_slide_deck, generate_patent_landscape, check_regulatory_compliance, compute_composite_risk, perform_global_sensitivity_analysis, integrate_arxiv_scraper, generate_data_package, monitor_performance, run_dashboard, integrate_cloud_lab, continuous_learning_loop, scale_performance, validate_predictions, generate_machine_readable_protocol, self_healing_loop, generate_audit_trail, ab_test_models, pilot_plant_cost_benefit, auto_git_commit, run_full_pipeline_with_target, generate_discovery_report, generate_scientific_explanation, simulate_cloud_lab, propagate_uncertainties, nsga2_optimization, expand_training_set, generate_research_paper, causal_discovery, llm_hypothesis_generation, cloud_lab_fallback_with_comparison, performance_profiling, submit_to_cloud_lab, adaptive_experimental_design, assimilate_experimental_data, external_validation, enhanced_patent_draft_generation, generate_technology_transfer_plan, supply_chain_risk_analysis, generate_regulatory_submission_package, lifecycle_assessment, study_superconducting_materials, generate_weekly_digest, send_email_notification, run_reproducibility_test, generate_project_health_report, validate_with_supercon, auto_retrain_on_new_data, streamlit_feedback, crystal_structure_prediction, topsis_ranking, fetch_arxiv_papers, extract_candidates_from_paper, screen_candidate, continuous_autonomous_loop, ModelVersionManager, MultiFidelityGP, SynthesisEnv, train_rl_agent, comprehensive_validation, data_assimilation_loop, CloudLabClient, CrystalStructurePredictor, MultiFidelityGP, run_validation, integrate_manufacturing_cost, run_benchmark, DataVersioning, self_optimize_pipeline, setup_logging, fetch_and_merge_materials_project_data, predict_tc_batch, generate_patent_draft, automated_document_sync, supercon_nims_integration, digital_twin_simulation, self_healing_mechanism, sobol_sensitivity_analysis, decision_support_system, reproducibility_check, factory_simulation, uncertainty_quantification, pipeline_benchmark, SynchrotronClient, ensemble_kalman_filter, automated_hypothesis_generation, patent_paper_generation, pdf_report_generation, high_throughput_screening_ternary_hydrides, generate_synthesis_protocol, data_quality_checks, expected_calibration_error, run_high_throughput_screening, validate_unified_model, generate_stakeholder_newsletter, run_sobol_analysis, decision_support, start_health_server, data_lineage_tracking, continuous_model_retraining, multi_objective_optimization, virtual_lab_simulation, integrate_arxiv_and_dft, generate_press_release, generate_monthly_report, generate_funding_proposal, generate_stakeholder_newsletter, run_sobol_analysis, decision_support, data_lineage_tracking, continuous_model_retraining, multi_objective_optimization, virtual_lab_simulation, reproducibility_check, integrate_arxiv_and_dft)",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor run_pipeline.py into separate modules by domain (e.g., run_pipeline/optimization.py, run_pipeline/simulation.py, run_pipeline/reporting.py, run_pipeline/validation.py, run_pipeline/cloud_lab.py, run_pipeline/supply_chain.py, run_pipeline/patent.py, run_pipeline/regulatory.py, run_pipeline/benchmark.py, run_pipeline/active_learning.py, run_pipeline/data_assimilation.py, run_pipeline/sensitivity.py, run_pipeline/decision_support.py, run_pipeline/reproducibility.py, run_pipeline/factory_simulation.py, run_pipeline/uncertainty_quantification.py, run_pipeline/hypothesis_generation.py, run_pipeline/patent_paper.py, run_pipeline/pdf_report.py, run_pipeline/screening.py, run_pipeline/calibration.py, run_pipeline/outreach.py, run_pipeline/sobol.py, run_pipeline/lineage.py, run_pipeline/retraining.py, run_pipeline/multi_objective.py, run_pipeline/virtual_lab.py, run_pipeline/integration.py, run_pipeline/press_release.py, run_pipeline/monthly_report.py, run_pipeline/funding_proposal.py, run_pipeline/newsletter.py, run_pipeline/health_server.py, run_pipeline/continuous_retraining.py, run_pipeline/reproducibility_check.py, run_pipeline/arxiv_integration.py) and import only what's needed. Remove all dead code."
  },
  {
    "issue": "requirements.txt only lists 6 packages (pytest, pytest-cov, numpy, scipy, matplotlib, pandas) but the code imports 50+ additional packages including streamlit, fastapi, uvicorn, torch, scikit-learn, plotly, websockets, apscheduler, stable-baselines3, gym, slowapi, prometheus-client, cryptography, pulp, GPy, simpy, SALib, networkx, aiohttp, skopt, mp-api, paho-mqtt, reportlab, joblib, pymatgen, torch-geometric, dowhy, openai, pymc3, psutil, schedule, seaborn, jupyter, ipython, black, flake8, mypy, httpx, python-multipart, aiofiles, mangum",
    "file": "requirements.txt",
    "severity": "high",
    "suggestion": "Add all missing dependencies to requirements.txt. Run `pip freeze` on a working environment to capture exact versions, or add the packages listed above with appropriate version constraints."
  },
  {
    "issue": "data/superconductor_database.json has inconsistent schema — some entries use 'name'/'Tc' keys, others use 'composition'/'tc' keys, and some use 'formula'/'predicted_Tc'. Multiple scripts (predict_tc.py, generate_candidates.py, run_pipeline.py) expect different key names, causing KeyError/ValueError at runtime",
    "file": "data/superconductor_database.json",
    "severity": "high",
    "suggestion": "Standardize the database schema to a single format (e.g., always use 'name' for compound name and 'Tc' for critical temperature). Write a migration script to convert all entries. Update all consuming scripts to use the same key names."
  },
  {
    "issue": "scripts/api_client.py is truncated mid-function — the file ends during the _request method definition, missing the rest of the class and all API endpoint methods",
    "file": "scripts/api_client.py",
    "severity": "high",
    "suggestion": "Complete the PipelineClient class by finishing the _request method and implementing all API endpoint methods (get_candidates, submit_candidate, batch_predict, etc.) as described in the docstring. Alternatively, remove the file if it's not needed."
  }
]
```

## Next actions

_(no actionable items detected — the deliverable may be analysis-only)_
