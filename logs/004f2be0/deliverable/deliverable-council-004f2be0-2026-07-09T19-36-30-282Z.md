# Council: directive answer

_3 drafters across 0/0 rounds — directive: "study superconducting materials (with online study if necessary) then please com…"_

> Generated by ollama_swarm preset: **council** · run 004f2be0-8e92-4776-8dfd-dba5a462effa · 2026-07-09T19:36:30.282Z

## Contents

- [Directive](#directive)
- [Answer to directive](#answer-to-directive)
- [Per-agent positions (latest)](#per-agent-positions-latest)
- [Round 0 — final drafts (full text)](#round-0-final-drafts-full-text)
- [Round 1 — independent first drafts (peer-hidden)](#round-1-independent-first-drafts-peer-hidden)
- [Next actions](#next-actions)

## Directive

study superconducting materials (with online study if necessary) then please come up with chemistry and physics to discover and manufacture room temperature superconducting compounds

## Answer to directive

Now I have all the verification I need. Here is the consolidated action plan:

```json
[
  {
    "description": "Fix dft_calculator.py: replace calls to undefined run_scf() and run_phonon_calculation() in run_dft_pipeline() with the existing run_full_dft_calculation() which already performs SCF + phonon + electron-phonon coupling in one call.",
    "expectedFiles": ["dft_calculator.py"]
  },
  {
    "description": "Fix scripts/generate_candidates.py: change 'from predict_tc import predict_tc' (line 17) to 'from scripts.predict_tc import predict_tc' to resolve ImportError since predict_tc lives in the scripts/ subdirectory.",
    "expectedFiles": ["scripts/generate_candidates.py"]
  },
  {
    "description": "Add empty __init__.py to scripts/ directory to make it a proper Python package, enabling 'from scripts.predict_tc import predict_tc' imports used by run_pipeline.py and app.py.",
    "expectedFiles": ["scripts/__init__.py"]
  },
  {
    "description": "Fix run_pipeline.py: change importlib.import_module('query_database'), importlib.import_module('generate_candidates'), importlib.import_module('predict_tc') to use fully qualified names with 'scripts.' prefix (e.g., importlib.import_module('scripts.query_database')).",
    "expectedFiles": ["run_pipeline.py"]
  },
  {
    "description": "Fix streamlit_dashboard.py: remove orphaned 'elif tab == \"System Health\":' and 'elif tab == \"Data Export\":' blocks inside collaboration_hub_tab() that reference undefined variable 'tab' and will raise NameError on import.",
    "expectedFiles": ["streamlit_dashboard.py"]
  },
  {
    "description": "Fix streamlit_dashboard.py: wrap module-level call to display_candidate_materials() (last line) inside 'if __name__ == \"__main__\":' guard to prevent execution on import before Streamlit is ready.",
    "expectedFiles": ["streamlit_dashboard.py"]
  },
  {
    "description": "Fix streamlit_dashboard.py: change what_if_analysis(parameters=parameters) call to match the actual function signature what_if_analysis(candidate_file: str = \"candidate_materials.md\", ...) which has no 'parameters' kwarg.",
    "expectedFiles": ["streamlit_dashboard.py"]
  },
  {
    "description": "Normalize data/superconductor_database.json schema to a single canonical format (consistent key names like 'name', 'Tc', 'lambda', 'omega_log', 'mu_star'), add crystal structure data (cell_parameters, atomic_positions, atomic_species) for hydride entries used by DFT calculations, and remove duplicate entries.",
    "expectedFiles": ["data/superconductor_database.json"]
  }
]
```

## Per-agent positions (latest)

### Agent 2

Now I have thoroughly verified all claims against the actual file contents. Here are my consolidated findings:

```json
[
  {
    "issue": "dft_calculator.py function run_dft_pipeline() (line 1036) calls undefined functions run_scf() and run_phonon_calculation() at lines 1062 and 1075 — neither function exists anywhere in the file, causing NameError at runtime.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Replace the calls to run_scf() and run_phonon_calculation(

### Agent 1

Now I have verified all claims against the actual file contents. Here is my consolidated analysis:

```json
[
  {
    "issue": "streamlit_dashboard.py has dangling `elif tab == \"System Health\":` and `elif tab == \"Data Export\":` blocks (lines 530-532) inside `collaboration_hub_tab()` function where `tab` is undefined — will raise NameError at runtime.",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Remove these orphaned elif blocks (lines 530-532) or wrap th

### Agent 3

Now I have all the verification I need. Here is my consolidated analysis:

```json
[
  {
    "issue": "dft_calculator.py function run_dft_pipeline() calls undefined run_scf() and run_phonon_calculation() at lines 1062 and 1075 — neither function exists anywhere in the file, causing NameError at runtime.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Replace run_dft_pipeline() with the existing run_full_dft_calculation() which already performs SCF + phonon + electro

## Round 0 — final drafts (full text)

_(no final-round drafts captured)_

## Round 1 — independent first drafts (peer-hidden)

### Agent 1

Now I have a thorough understanding of the codebase. Here are my findings:

```json
[
  {
    "issue": "run_pipeline.py is a monolithic 10,000+ line file containing dozens of duplicate function definitions, multiple FastAPI apps, Streamlit dashboard code, and conflicting entry points — making it unmaintainable and impossible to import without side effects.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Split run_pipeline.py into separate modules: one for the pipeline orchestration, one for the FastAPI API server, one for utility functions. Remove duplicate definitions (sobol_sensitivity_analysis appears at least 3 times, generate_discovery_announcement appears twice, generate_research_paper appears twice)."
  },
  {
    "issue": "scripts/predict_tc.py defines `predict_tc()` as both a standalone function (line ~200) and as a class method in `BCSPINN` (line ~700), causing namespace collision. The module also has 4 different `main_*` entry points (`main`, `main_screening`, `main_pinn`, `main_gnn`) with conflicting `if __name__ == '__main__'` blocks.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Rename the standalone `predict_tc()` function to `predict_tc_from_db()` to avoid shadowing. Consolidate the four main entry points into a single `main()` with subcommands. Remove the duplicate `if __name__ == '__main__'` blocks at the bottom."
  },
  {
    "issue": "streamlit_dashboard.py imports `live_external_validation`, `what_if_analysis`, `generate_press_release`, `generate_monthly_report` from `run_pipeline` but these functions have incompatible signatures (e.g., `live_external_validation()` is called with no args but defined with 3 required args). The dashboard also has a bare `display_candidate_materials()` call at module level outside any function, which will execute on import.",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Fix the import signatures to match the actual function definitions. Wrap the `display_candidate_materials()` call inside a function or guard it with `if __name__ == '__main__'`. Remove the dangling `elif tab == \"System Health\":` and `elif tab == \"Data Export\":` blocks that reference undefined variables."
  },
  {
    "issue": "dft_calculator.py imports `torch_geometric`, `shap`, `ase`, `sklearn.ensemble.RandomForestRegressor` at the top level but these are heavy dependencies that may not be installed. The file also defines `compute_tc()` twice (once as a standalone function, once as `compute_tc_mcmillan_allen_dynes`), and `structure_to_graph()` is duplicated in both dft_calculator.py and predict_tc.py.",
    "file": "dft_calculator.py",
    "severity": "medium",
    "suggestion": "Move heavy imports inside the functions that use them (lazy imports). Remove the duplicate `compute_tc()` function. Extract `structure_to_graph()` into a shared utility module to avoid duplication."
  },
  {
    "issue": "The `scripts/` directory lacks `__init__.py` files, so `from scripts.predict_tc import predict_tc` (used in run_pipeline.py and app.py) will raise `ModuleNotFoundError` or `ImportError` in Python.",
    "file": "scripts/__init__.py",
    "severity": "high",
    "suggestion": "Add an empty `__init__.py` file to the `scripts/` directory to make it a proper Python package. Also add `__init__.py` to `tests/` and `data/` if they are used as packages."
  },
  {
    "issue": "app.py defines a minimal FastAPI app with in-memory storage and a dummy batch_predict endpoint (`20 + 10 * h_count` heuristic), but run_pipeline.py defines a completely separate FastAPI app with different endpoints, authentication, and rate limiting. This creates two conflicting API servers with no shared state.",
    "file": "app.py",
    "severity": "medium",
    "suggestion": "Consolidate the two FastAPI apps into a single `api.py` module. Either import the run_pipeline app into app.py or vice versa. Remove the duplicate in-memory storage and dummy prediction logic from app.py."
  }
]
```

### Agent 2

Now I have a thorough understanding of the codebase. Here are my findings:

```json
[
  {
    "issue": "run_pipeline.py is a ~10,000+ line monolithic file with massive code duplication — functions like sobol_sensitivity_analysis(), reproducibility_check(), generate_discovery_announcement(), generate_press_release(), generate_research_paper(), digital_twin_simulation(), validate_with_supercon(), generate_reproducibility_package(), generate_discovery_report(), generate_materials_physics_report(), generate_technology_transfer_plan(), supply_chain_risk_analysis(), generate_regulatory_submission_package(), lifecycle_assessment(), study_superconducting_materials(), generate_weekly_digest(), generate_project_health_report(), generate_project_closure_report(), controversy_analysis(), data_consistency_validation(), watch_mode(), high_throughput_screening(), CloudLabAPIClient, active_learning_bayesian_opt, multi_objective_weighted_sum, multi_objective_nsga2, validate_model, retrain_active_learning, VirtualLabSimulator, assimilate_experimental_data, run_closed_loop_simulation, watch_and_retrain, MultiFidelityGP, SynthesisEnv, train_rl_agent, comprehensive_validation, data_assimilation_loop, CloudLabClient, CrystalStructurePredictor, run_validation, integrate_manufacturing_cost, run_benchmark, DataVersioning, self_optimize_pipeline, setup_logging, fetch_and_merge_materials_project_data, predict_tc_batch, generate_patent_draft, automated_document_sync, supercon_nims_integration, self_healing_mechanism, decision_support_system, factory_simulation, uncertainty_quantification, pipeline_benchmark, SynchrotronClient, ensemble_kalman_filter, automated_hypothesis_generation, patent_paper_generation, pdf_report_generation, high_throughput_screening_ternary_hydrides, generate_synthesis_protocol, data_quality_checks, expected_calibration_error, run_high_throughput_screening, start_health_server, data_lineage_tracking, continuous_model_retraining, multi_objective_optimization, virtual_lab_simulation, integrate_arxiv_and_dft, generate_monthly_report, generate_funding_proposal, generate_stakeholder_newsletter, run_sobol_analysis, decision_support, validate_unified_model, generate_press_release, generate_monthly_report, generate_funding_proposal, generate_stakeholder_newsletter, run_sobol_analysis, decision_support, data_lineage_tracking, continuous_model_retraining, multi_objective_optimization, virtual_lab_simulation, integrate_arxiv_and_dft are all defined 2–5 times each. This makes the file unmaintainable and causes the last definition to silently override all prior ones.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor run_pipeline.py into separate modules by concern (e.g., pipeline_orchestrator.py, ml_models.py, dft_integration.py, api.py, reporting.py, sensitivity.py, simulation.py, etc.) and remove all duplicate function definitions. Each function should be defined exactly once."
  },
  {
    "issue": "dft_calculator.py calls undefined functions run_scf() and run_phonon_calculation() in the run_dft_pipeline() function (lines near the end of the file). These functions are not defined anywhere in the file, so calling run_dft_pipeline() will raise a NameError at runtime.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Implement run_scf() and run_phonon_calculation() functions, or remove the run_dft_pipeline() wrapper and use the existing run_full_dft_calculation() which already performs SCF + phonon + electron-phonon coupling."
  },
  {
    "issue": "app.py defines a separate FastAPI application (title='Superconductivity Pipeline API') while run_pipeline.py also defines a FastAPI application (title='Superconductor Discovery API') with overlapping endpoints (/candidates, /health, /submit, /batch_predict). This creates a conflict — two separate API servers with different routes and authentication schemes in the same project.",
    "file": "app.py",
    "severity": "high",
    "suggestion": "Consolidate into a single FastAPI app. Either remove app.py and use the app defined in run_pipeline.py, or move the run_pipeline.py API into app.py and remove the duplicate app definition from run_pipeline.py."
  },
  {
    "issue": "scripts/predict_tc.py has duplicate function definitions: train_gp_model() is defined twice (lines ~500 and ~800), structure_to_graph() is defined twice (lines ~600 and ~700), generate_candidates() is defined twice (lines ~650 and ~670), predict_with_uncertainty() is defined twice (lines ~550 and ~680), and main() is defined multiple times. The second definition silently overrides the first, making the first definition dead code.",
    "file": "scripts/predict_tc.py",
    "severity": "medium",
    "suggestion": "Remove all duplicate function definitions. Each function should appear exactly once. Consolidate the two train_gp_model() variants, the two structure_to_graph() variants, etc."
  },
  {
    "issue": "dft_calculator.py imports torch_geometric (via from torch_geometric.data import Data, from torch_geometric.nn import GCNConv, global_mean_pool) and shap, which are extremely heavy dependencies. The GNN code is never actually used in the DFT pipeline — the MLTcPredictor class is instantiated but its GNN training method (train_gnn) is never called from any pipeline code. This adds ~500MB+ of unnecessary dependency weight.",
    "file": "dft_calculator.py",
    "severity": "medium",
    "suggestion": "Move the GNN and SHAP code into a separate ml_models.py module that is only imported when needed. Remove torch_geometric and shap imports from dft_calculator.py, which should only contain DFT/physics-based calculations."
  },
  {
    "issue": "requirements.txt includes many heavy dependencies that are barely used or not used at all: torch-geometric (~500MB), stable-baselines3, GPy, simpy, SALib, pulp, cryptography, prometheus-client, slowapi, paho-mqtt, reportlab, apscheduler, weasyprint, flask, pyvisa, mangum. Most of these are imported in run_pipeline.py but only used in stub/placeholder functions that are never called in the actual pipeline flow. This makes the environment bloated and installation slow.",
    "file": "requirements.txt",
    "severity": "medium",
    "suggestion": "Split requirements.txt into core_requirements.txt (numpy, scipy, scikit-learn, pandas, matplotlib, fastapi, uvicorn, streamlit, requests, pydantic) and optional_requirements.txt (torch-geometric, stable-baselines3, GPy, simpy, SALib, etc.). Only install optional dependencies when the corresponding feature is enabled."
  }
]
```

### Agent 3

Here are my findings as a structured JSON array:

```json
[
  {
    "issue": "run_pipeline.py is a monolithic ~10,000+ line file with massive code duplication — functions like sobol_sensitivity_analysis, generate_discovery_announcement, generate_research_paper, submit_to_arxiv, real_time_collaboration, validate_with_supercon, and integrate_real_cloud_lab are each defined 2–3 times with different implementations, causing name collisions where the last definition silently overrides earlier ones.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor into separate modules (e.g., scripts/sobol.py, scripts/validation.py, scripts/cloud_lab.py) and remove all duplicate function definitions. Use a single source of truth for each function."
  },
  {
    "issue": "run_pipeline.py imports dozens of packages that are not listed in requirements.txt and likely not installed (GPy, simpy, SALib, paho-mqtt, dowhy, openai, pymc3, torch_geometric, mp_api, stable_baselines3, pyvisa, flask, weasyprint, apscheduler, etc.), causing ImportError at runtime. The file also imports torch_geometric and shap which are listed in requirements.txt but not actually installed in a typical environment.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Either add all imported packages to requirements.txt with pinned versions, or wrap all optional imports in try/except blocks with graceful fallbacks. Remove imports for packages that are never actually used."
  },
  {
    "issue": "run_pipeline.py main() function tries to import query_database, generate_candidates, and predict_tc as top-level modules via importlib.import_module(), but these modules live in the scripts/ subdirectory. The import will fail at runtime because the scripts/ directory is not in sys.path and the module names are not fully qualified.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Change imports to use fully qualified names: importlib.import_module('scripts.query_database'), importlib.import_module('scripts.generate_candidates'), importlib.import_module('scripts.predict_tc'). Or add scripts/ to sys.path before importing."
  },
  {
    "issue": "scripts/generate_candidates.py does `from predict_tc import predict_tc` at the top of the file, which will fail when run from the scripts/ directory because predict_tc is not a top-level module — it's scripts.predict_tc. This causes an ImportError on every execution of generate_candidates.py.",
    "file": "scripts/generate_candidates.py",
    "severity": "high",
    "suggestion": "Change to `from scripts.predict_tc import predict_tc` or use a relative import `from .predict_tc import predict_tc` if running as a package. Alternatively, add the parent directory to sys.path."
  },
  {
    "issue": "data/superconductor_database.json has inconsistent schema across entries — some use 'name' + 'Tc' keys (e.g., {'name': 'HgBa2Ca2Cu3O8', 'Tc': 135}), others use 'composition' + 'tc' (e.g., {'composition': 'FeSe', 'tc': 8}), and many entries are missing required fields like 'lambda', 'omega_log', and 'mu_star' that scripts/predict_tc.py and dft_calculator.py depend on. This causes KeyError or ValueError at runtime.",
    "file": "data/superconductor_database.json",
    "severity": "medium",
    "suggestion": "Normalize the database schema to a single consistent format (e.g., always use 'name', 'Tc', 'lambda', 'omega_log', 'mu_star'). Add a schema validation step before any code reads the database. Migrate all entries to the canonical format."
  },
  {
    "issue": "dft_calculator.py function compute_dft_properties_for_top_candidates() assumes database entries contain 'cell_parameters', 'atomic_positions', and 'atomic_species' keys, but the actual data/superconductor_database.json entries do not have these keys — they only have metadata like 'name', 'Tc', 'pressure', 'structure', etc. This will cause KeyError at runtime.",
    "file": "dft_calculator.py",
    "severity": "medium",
    "suggestion": "Either add crystal structure data to the database entries, or change compute_dft_properties_for_top_candidates() to generate placeholder structures from the chemical formula (e.g., using pymatgen's Composition and Structure classes) rather than reading non-existent keys from the database."
  }
]
```

## Next actions

**MEDIUM priority:**
- "suggestion": "Refactor run_pipeline.py into separate modules by concern (e.g., pipeline_orchestrator.py, ml_models.py, dft_integration.py, api.py, reporting.py, sensitivity.py, simulation.py, etc.) and remove all duplicate function definitions. Each function should be defined exactly once." _(from: Agent 2)_
- "suggestion": "Remove all duplicate function definitions. Each function should appear exactly once. Consolidate the two train_gp_model() variants, the two structure_to_graph() variants, etc." _(from: Agent 2)_
- "suggestion": "Move the GNN and SHAP code into a separate ml_models.py module that is only imported when needed. Remove torch_geometric and shap imports from dft_calculator.py, which should only contain DFT/physics-based calculations." _(from: Agent 2)_
