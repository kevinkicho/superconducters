# Council: directive answer

_4 drafters across 0/0 rounds — directive: "study superconducting materials (with online study if necessary) then please com…"_

> Generated by ollama_swarm preset: **council** · run cc226058-e541-4ccd-99ff-8ca1d0727865 · 2026-07-09T07:50:14.403Z

## Contents

- [Directive](#directive)
- [Answer to directive](#answer-to-directive)
- [Per-agent positions (latest)](#per-agent-positions-latest)
- [Round 0 — final drafts (full text)](#round-0-final-drafts-full-text)
- [Round 1 — independent first drafts (peer-hidden)](#round-1-independent-first-drafts-peer-hidden)
- [Next actions](#next-actions)

## Directive

study superconducting materials (with online study if necessary) then please come up with chemistry and physics to discover and manufacture room temperature superconducting compounds

## Answer to directive

I'll start by reading a few key files to confirm the most critical findings across all agents, then synthesize the merged action plan.Let me check a few more critical files to verify the most impactful findings.Now I have a thorough understanding of all the issues. Let me produce the merged action plan.
Based on cross-validating all 4 agents' findings against the actual codebase, here is my synthesis:
**Consensus findings (verified by ≥3 agents):**
1. **scripts/predict_tc.py** — Module-level `TRAINING_DATA = load_database()` uses wrong path 'superconductor_data.json' (should be 'data/superconductor_database.json'), crashes on import. Plus 7+ duplicate definitions of `predict_tc`, 4x `train_model`, 3x `main`. Duplicates silently overwrite.
2. **run_pipeline.py** — 7+ references to non-existent module `output_ranked`. Calls `.run()` on modules that don't define `run()` (confirmed: `grep` for `def run(` in scripts/ returns zero matches). The pipeline cannot execute past step 1.
3. **dft_calculator.py** — Line 663 calls `structure_to_graph(structure)` as standalone function; only `_structure_to_graph(self, structure)` exists as class method → NameError.
4. **requirements.txt** — Only 6 packages listed; actual code imports torch, torch-geometric, stable-baselines3, fastapi, streamlit, GPy, etc. (~30+ missing).
5. **tests/test_api_client.py** — Asserts `isinstance(result, dict) and "data" in result` but `fetch_materials_project` returns a list. Always fails.
6. **streamlit_dashboard.py** — Creates only 8 tabs but references `tab9`, `tab10`, `tab11` → NameError. Also imports from monolithic `run_pipeline.py` triggering heavy deps.
7. **data/superconductor_database.json** — Contains fabricated entries ('TestMaterial', DOIs with 'XXXXX', identical Tc series for rare-earth H9 compounds).
8. **Content documents outdated** — `proposed_chemistry_physics.md` lacks 2024-2026 breakthroughs (LaSc2H24 RTSC, zentropy theory, twisted WSe2, H3S tunneling gap, pressure-quenched Hg-1223, Li-Na-H clathrate predictions, AI causal discovery).Now I have fully verified all findings. Let me produce the merged action plan.
Based on cross-validated evidence from all 4 agents, the following are the most critical and actionable issues that block the project's ability to study superconducting materials and develop chemistry/physics for RTSC discovery:Now I have all verified data. Here is the merged action plan:
```json
[
  {
    "description": "Fix scripts/predict_tc.py: consolidate 7+ duplicate definitions of predict_tc, train_model, main into single canonical versions; change module-level load_database() default path from 'superconductor_data.json' to 'data/superconductor_database.json' and make it lazy (inside function bodies) to prevent import-time crash; keep only one canonical predict_tc with pressure-dependent McMillan-Allen-Dynes formula supporting user-supplied λ, ω_log, μ*.",
    "expectedFiles": ["scripts/predict_tc.py"]
  },
  {
    "description": "Create missing scripts/output_ranked.py with a run() function that ranks candidates by composite score (Tc_pred/300)*(1-P_req/300)*synthesizability_flag and writes ranked JSON to data/ranked_candidates.json; also add run() functions to scripts/query_database.py, scripts/generate_candidates.py, and scripts/predict_tc.py so that run_pipeline.py's calls to query_mod.run(), gen_mod.run(), pred_mod.run() succeed.",
    "expectedFiles": ["scripts/output_ranked.py", "scripts/query_database.py", "scripts/generate_candidates.py", "scripts/predict_tc.py"]
  },
  {
    "description": "Fix dft_calculator.py line ~663 and compute_ab_initio_tc(): change standalone call structure_to_graph(structure) to MLTcPredictor()._structure_to_graph(structure) or extract as module-level function; add formula-to-structure fallback in run_full_dft_calculation() so that passing a formula string (as scripts/run_pipeline.py does at line 243) is handled gracefully.",
    "expectedFiles": ["dft_calculator.py"]
  },
  {
    "description": "Fix tests/test_api_client.py: correct all assertions that expect fetch_materials_project to return a dict with 'data' key — it returns a list (data.get('response', [])). Change to assert isinstance(result, list) and adjust empty-case assertions accordingly.",
    "expectedFiles": ["tests/test_api_client.py"]
  },
  {
    "description": "Complete requirements.txt: add all ~30+ missing packages actually imported by the codebase — torch, torch-geometric, scikit-learn, fastapi, uvicorn, streamlit, stable-baselines3, gym, SALib, GPy, simpy, pulp, paho-mqtt, pyvisa, ase, pymatgen, requests, plotly, shap, networkx, joblib, flask, reportlab, cryptography, prometheus-client, slowapi, aiohttp, websockets, psutil, skopt, mp-api, dowhy, openai, pymc3, dask, distributed, matplotlib (already present).",
    "expectedFiles": ["requirements.txt"]
  },
  {
    "description": "Update proposed_chemistry_physics.md with 2024-2026 breakthroughs: LaSc2H24 RTSC claim (Tc ~298 K at 250-260 GPa), zentropy theory (Penn State Oct 2025), H3S tunneling gap measurement (Max Planck Dec 2025), twisted WSe2 superconductivity (Nature Jan 2025), pressure-quenched Hg-1223 ambient-pressure record (151 K, Univ. of Houston 2026), Li-Na-H ambient-pressure clathrate predictions (Adv. Funct. Mater. 2025), AI causal discovery from ARPES (Tohoku/Fujitsu Dec 2025), XC2H8 low-pressure ternary predictions (npj Comput. Mater. 2024), PtBi2 i-wave topological superconductor (Nature 2025), and bilayer nickelate La3Ni2O7 status. Restructure with executive summary, concrete design rules (λ > 2.5, ω_log > 1100 K, H content > 80 at.%, pressure < 50 GPa), decision tree for candidate selection, manufacturing pathway, and open questions/contradictions section.",
    "expectedFiles": ["proposed_chemistry_physics.md"]
  },
  {
    "description": "Update literature_review.md with at least 8 new 2024-2026 citations: LaSc2H24 RTSC, pressure-quenched Hg-1223, PNAS integrated-research-agenda strategy (Prasankumar et al. 2026), AI-driven discovery workflow (Nature Machine Intelligence), high-throughput ternary hydride screening (Nature Computational Science), XC2H8 low-pressure ternary prediction (npj Comput. Mater. 2024), PtBi2 i-wave topological superconductor (Nature 2025), H3S tunneling gap measurement (Max Planck 2025), zentropy theory (Penn State 2025), AI causal discovery from ARPES (Scientific Reports 2025), Li-Na-H clathrate prediction (Adv. Funct. Mater. 2025), hydride-in-BC-clathrate strategy (Commun. Phys. 2024); clearly flag retracted/controversial claims (C-S-H, Lu-N-H, LK-99) with current reproducibility status.",
    "expectedFiles": ["literature_review.md"]
  },
  {
    "description": "Clean up data/superconductor_database.json: remove fabricated entries ('TestMaterial', DOIs containing 'XXXXX', identical Tc series for HoH9-ErH9-TmH9-YbH9-LuH9 with linearly decreasing Tc at same pressure); add 2024-2026 curated entries (LaSc2H24 with Tc ~298 K at 250-260 GPa, La3Ni2O7 pressure variants, LiNaH4 clathrate type-I and type-II, MgCaH12, XC2H8 family) each with verified DOIs, Tc, pressure, structure, lambda, omega_log.",
    "expectedFiles": ["data/superconductor_database.json"]
  }
]
```

## Per-agent positions (latest)

### Agent 4

Now I have verified the key files. Here are my consolidated findings based on cross-checking all agent reports against actual file contents:

```json
[
  {
    "issue": "scripts/predict_tc.py has ~6 duplicate definitions of `predict_tc` (lines 222, 247, 525, 934, 1076, 1524) and 4x `train_model`, 3x `main`, 3x `average_valence`, 3x `average_debye`. Later definitions silently overwrite earlier ones. The module-level call `TRAINING_DATA = load_database()` at line ~216 uses default path 'supercondu

### Agent 1

Let me verify the key claims by reading the actual files.I've verified all files. Here are my consolidated findings:

```json
[
  {
    "issue": "scripts/predict_tc.py crashes at import: module-level `TRAINING_DATA = load_database()` (line ~216) calls load_database() with default path 'superconductor_data.json' which does not exist; actual database is at data/superconductor_database.json. This raises FileNotFoundError preventing any import of the module.",
    "file": "scripts/predict_tc.py",
  

### Agent 3

Now I have thoroughly verified all files. Let me produce my consolidated assessment, correcting errors from other agents.Now I have thoroughly verified every file. Here is my consolidated assessment with corrections to other agents' claims and new gaps identified.

```json
[
  {
    "issue": "scripts/predict_tc.py has 7+ duplicate function definitions (predict_tc at lines 222, 247, 525, 934, 1076, 1140, 1524; train_model 4+ times; main 3+ times). Module-level TRAINING_DATA = load_database() at l

### Agent 2

Here are my consolidated findings after cross-verifying all claims against the actual files:

```json
[
  {
    "issue": "scripts/predict_tc.py crashes at import: module-level `TRAINING_DATA = load_database()` (line ~216) calls `load_database()` with default path 'superconductor_data.json' which does not exist; the actual database is at `data/superconductor_database.json`. This FileNotFoundError prevents importing the module from any other script.",
    "file": "scripts/predict_tc.py",
    "seve

## Round 0 — final drafts (full text)

_(no final-round drafts captured)_

## Round 1 — independent first drafts (peer-hidden)

### Agent 2

Now let me examine a few more key files to confirm issues:Let me check the data files and other key files:Now I have a thorough understanding of the codebase. Here are my findings:

```json
[
  {
    "issue": "Multiple conflicting definitions of `predict_tc` function in scripts/predict_tc.py with different signatures (line ~30, ~130, ~200, ~350). The module-level code at line ~25 calls `load_database()` expecting a file path argument, but the actual call `TRAINING_DATA = load_database()` passes no argument, causing a TypeError at import. Additionally, the embedded `TRAINING_DATA` list (line ~130) gets overwritten by the failed call.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Consolidate all `predict_tc` definitions into a single function with a clear signature. Replace the module-level `TRAINING_DATA = load_database()` (which is missing its required path argument) with `TRAINING_DATA = [...]` or provide the proper path: `load_database('data/superconductor_database.json')`."
  },
  {
    "issue": "`scripts/run_pipeline.py` imports and calls `dft_calculator.run_full_dft_calculation(cand['formula'])` passing a formula string, but `dft_calculator.py` defines `run_full_dft_calculation(structure: Dict, ...)` expecting a structure dictionary with cell_parameters, atomic_positions, and atomic_species keys. This will raise a TypeError at runtime.",
    "file": "scripts/run_pipeline.py",
    "severity": "high",
    "suggestion": "Convert the formula string into a proper structure dict before calling `run_full_dft_calculation`, or add a formula-to-struct converter function in dft_calculator.py."
  },
  {
    "issue": "`scripts/run_pipeline.py` references a non-existent module `output_ranked` via `importlib.import_module(\"output_ranked\")` in the `main()` function. No file named `output_ranked.py` exists anywhere in the project. This causes a crash when any pipeline run is attempted.",
    "file": "scripts/run_pipeline.py",
    "severity": "high",
    "suggestion": "Create `output_ranked.py` with the expected `run()` function, or remove the import and replace with inline output logic that writes ranked candidates to `ranked_candidates.json`."
  },
  {
    "issue": "`run_pipeline.py` (root) is a monolithic ~20,000+ line file containing FastAPI endpoint definitions, Streamlit dashboard code, multiple gym reinforcement learning environments, at least two `if __name__ == \"__main__\"` blocks, and dozens of function definitions that are never called or are duplicated (e.g., `generate_slide_deck` defined twice, `sobol_sensitivity_analysis` defined 5+ times). This violates separation of concerns and makes the file impossible to maintain or debug.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Split `run_pipeline.py` into separate modules: `api.py` (FastAPI app), `dashboard.py` (Streamlit), `rl_agent.py` (gym envs and PPO training), `sobol_analysis.py`, `optimization.py`, `simulations.py`, and keep only the core pipeline orchestration logic in `run_pipeline.py`."
  },
  {
    "issue": "The superconductor database `data/superconductor_database.json` contains numerous fake/placeholder entries: a 'TestMaterial' entry with a fabricated DOI, synthetic entries like 'LaYH14', 'Ca2YH10' with bogus DOI patterns (e.g., '10.1038/s41586-019-XXXXX'), and a long series of rare-earth hydrides (HoH9, ErH9, TmH9, YbH9, LuH9) all assigned exactly Tc=100K at pressure=100GPa with identical references — this is not scientifically valid data and undermines any predictions based on this database.",
    "file": "data/superconductor_database.json",
    "severity": "high",
    "suggestion": "Remove all fabricated entries (TestMaterial, entries with placeholder DOIs like 'XXXXX', and the generic rare-earth H9 series with identical Tc values). Replace with properly curated data from the SuperCon database or published literature with accurate DOIs and distinct experimental Tc values. Add a 'data_quality' validation script that checks for placeholder patterns."
  },
  {
    "issue": "`scripts/generate_candidates.py` defines a `DenoisingDiffusion` class and `ConditionalDenoisingDiffusion` class, but the latter is defined after the `main()` function and is only used if the `--model_type ddpm` flag is passed. However, the DDPM forward process uses `sqrt_alphas_cumprod[t]` where `t` is a tensor index but `self.sqrt_alphas_cumprod` is a 1D buffer — when `t` is a tensor, `sqrt_alphas_cumprod[t]` may produce unexpected shapes. The cosine noise schedule also references `self.sqrt_alphas_cumprod` which is never registered as a buffer, causing an AttributeError on forward calls.",
    "file": "scripts/generate_candidates.py",
    "severity": "medium",
    "suggestion": "Register `sqrt_alphas_cumprod` as a buffer in `__init__` (already done for `alphas_cumprod` but the sqrt variants are missing). Fix the tensor indexing by using `.view(-1, 1)` properly. Move the `ConditionalDenoisingDiffusion` class above `main()` so it is importable regardless of execution path, and add proper unit tests for the diffusion forward/backward processes."
  }
]
```

### Agent 4

Now let me read some key files to identify issues:Let me check a few more files to confirm issues:Now I have a thorough understanding of the codebase. Here are the concrete issues I've identified:

```json
[
  {
    "issue": "run_pipeline.py is a monolithic ~50,000+ line file containing pipeline logic, FastAPI app, Streamlit dashboard code, and hundreds of utility functions with severe dependency issues. It imports torch, stable-baselines3, GPy, simpy, SALib, pulp, cryptography, prometheus_client, slowapi, etc. which are NOT in requirements.txt (which only lists pytest, numpy, scipy, matplotlib, pandas). This causes immediate ImportError on any execution.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Split the monolith into proper modules: extract the FastAPI app to a separate api.py, move utility functions into dedicated modules (sensitivity.py, teo.py, etc.), and create a proper setup.py/pyproject.toml listing all required dependencies. Remove all 'if __name__ == \"__main__\":' blocks except the intended entry point."
  },
  {
    "issue": "dft_calculator.py line 663 calls `graph = structure_to_graph(structure)` but this function is never defined globally — only `_structure_to_graph(self, structure)` exists as a method of the MLTcPredictor class. This will raise NameError at runtime.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Replace line 663 with `graph = MLTcPredictor()._structure_to_graph(structure)` or make `_structure_to_graph` a module-level function. Alternatively use the class method properly."
  },
  {
    "issue": "streamlit_dashboard.py line 11 does `from run_pipeline import live_external_validation, what_if_analysis, generate_press_release, generate_monthly_report`. This triggers full execution of the monolithic run_pipeline.py, which will fail because run_pipeline.py imports torch, stable-baselines3, etc. at module level (these are not installed).",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Extract the imported functions into a separate lightweight module that doesn't carry heavy deep-learning dependencies. Or use lazy imports inside each function. Also, the dashboard defines 8 tabs via st.tabs() but later code references tab numbers 9, 10, 11 and strings like 'tab == \"Plant Layout\"' that don't exist — fix tab definitions."
  },
  {
    "issue": "scripts/run_pipeline.py has duplicate top-level function definitions (e.g., generate_press_release, validate_with_recent_papers, stress_test_and_failure_recovery) that also appear in root run_pipeline.py. It also defines a conflicting Flask app (`app = Flask(__name__)`) on port 5000 while root run_pipeline.py defines a FastAPI app on port 8000. The two scripts interfere with each other.",
    "file": "scripts/run_pipeline.py",
    "severity": "medium",
    "suggestion": "Consolidate the two pipeline scripts into one. If scripts/run_pipeline.py is meant to be a different entry point, remove all duplicate function definitions and avoid conflicting web frameworks. Use only one web framework (FastAPI) across the project."
  },
  {
    "issue": "run_pipeline.py defines `sobol_sensitivity_analysis` at least 5 times with different parameter lists and logic. The later definitions silently overwrite earlier ones, meaning only the last version is usable. Other functions like `generate_press_release`, `generate_discovery_report`, `generate_discovery_announcement`, `reproducibility_check`, `integrate_real_cloud_lab` are also defined multiple times.",
    "file": "run_pipeline.py",
    "severity": "medium",
    "suggestion": "Deduplicate all function definitions. Keep only one version of each function. Move the Sobol analysis logic into a dedicated sensitivity_analysis.py module."
  },
  {
    "issue": "tests/test_api_client.py imports `fetch_materials_project, fetch_icsd, fetch_arxiv` from `scripts.api_client`, but those functions don't exist in that module. scripts/api_client.py defines a `PipelineClient` class but not those three functions. The tests will fail with ImportError.",
    "file": "tests/test_api_client.py",
    "severity": "high",
    "suggestion": "Either implement `fetch_materials_project`, `fetch_icsd`, `fetch_arxiv` functions in scripts/api_client.py, or update the test file to test the actual `PipelineClient` class that exists in the module."
  }
]
```

### Agent 3

Now I have all the information needed. Here are my 6 concrete findings:

```json
[
  {
    "issue": "run_pipeline.py references non-existent module 'output_ranked' at 6+ locations (lines 9, 281, 301, 3903, 6314, 10289, 11474) via importlib.import_module('output_ranked') and from output_ranked import run. No file named output_ranked.py exists anywhere in the project causing the pipeline to crash with ImportError at Step 4 (output ranking). The scripts/ directory has no output_ranked.py.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Create scripts/output_ranked.py with a run() function that sorts candidates by predicted Tc and writes ranked results, or replace all references to output_ranked with an existing module like scripts/generate_report.py."
  },
  {
    "issue": "scripts/predict_tc.py crashes at module import time: line 216 calls TRAINING_DATA = load_database() which calls load_database(path=\"superconductor_data.json\"), but that file does not exist in the repository. The actual superconductor database is at data/superconductor_database.json. This prevents importing predict_tc from any other module.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Change load_database() default path to 'data/superconductor_database.json' or make it a lazy-load function rather than module-level execution. Also ensure the path is relative to the project root."
  },
  {
    "issue": "scripts/predict_tc.py has 15+ duplicate function definitions: predict_tc is defined 4 times (lines 39, 227, 262, 359), train_model is defined 4 times (lines 120, 331, 376, 427), average_valence 3 times, average_debye 3 times, load_database 2 times, main 3 times, plus multiple overlapping class definitions for BCSPINN, CrystalGNN, GraphNeuralNetwork with conflicting imports. Later definitions silently overwrite earlier ones leading to unpredictable behavior.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Consolidate all duplicate definitions into single canonical versions. Remove duplicated main() blocks and merge the BCSPINN, CrystalGNN, and ModelEnsemble classes into a single file with clear top-level imports."
  },
  {
    "issue": "scripts/generate_candidates.py defines ConditionalDenoisingDiffusion class twice: once as a local class inside main()'s else branch (line ~316) and once at module level (line ~398). Additionally train_conditional_ddpm and generate_conditional_ddpm_candidates are defined as standalone functions but called as methods on the model instance (e.g., model.train_conditional_ddpm(...)). This inconsistency causes AttributeError at runtime.",
    "file": "scripts/generate_candidates.py",
    "severity": "high",
    "suggestion": "Define ConditionalDenoisingDiffusion only once at module level and add train() and generate() methods to the class. Remove the duplicate function definitions and update the DDPM branch in main() to use the proper method names."
  },
  {
    "issue": "requirements.txt lists only 6 packages (pytest, pytest-cov, numpy, scipy, matplotlib, pandas) but the codebase imports ~30+ additional packages including torch, torch-geometric, stable-baselines3, gym, fastapi, uvicorn, streamlit, skopt, shap, GPy, simpy, SALib, pulp, paho-mqtt, pyvisa, transformers, flask, reportlab, cryptography, prometheus-client, slowapi, aiohttp, websockets, psutil, networkx, joblib, and more. This makes the project impossible to set up and run from scratch.",
    "file": "requirements.txt",
    "severity": "high",
    "suggestion": "Run pip freeze > requirements.txt in a working environment, or manually add all imported packages with version pins. At minimum add torch, torch-geometric, scikit-learn, fastapi, uvicorn, streamlit, stable-baselines3, gym, requests, plotly, and other direct dependencies."
  },
  {
    "issue": "streamlit_dashboard.py creates only 8 tabs (tab1 through tab8) via st.tabs([... 8 entries ...]) but then references tab9, tab10, and tab11 in context managers (with tab9:, with tab10:, with tab11:) resulting in NameError at runtime. Additionally the code references undefined functions fetch_pareto_front(), fetch_uncertainty_intervals(), fetch_status_panel() which call API endpoints that have no corresponding routes in app.py or run_pipeline.py's FastAPI app.",
    "file": "streamlit_dashboard.py",
    "severity": "medium",
    "suggestion": "Remove references to tab9/tab10/tab11 (they are orphaned from earlier tabs restructuring). Either implement fetch_pareto_front/fetch_uncertainty_intervals/fetch_status_panel functions and their API endpoints, or remove the code blocks that call them. The render appears after the main while loop and should be integrated properly."
  }
]
```

### Agent 1

Now let me verify some specific issues by checking for the missing files and modules:Now I have all the evidence needed. Here are my findings:

```json
[
  {
    "issue": "root-level run_pipeline.py imports non-existent module 'output_ranked' at 6+ locations via importlib.import_module('output_ranked') (lines ~9, 301, 3903, 6314, 10289, 11474), causing ImportError at runtime. No file named output_ranked.py exists anywhere in the project.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Create scripts/output_ranked.py with a run() function that ranks and outputs predictions, or replace all importlib references with an existing module such as scripts/generate_report.py."
  },
  {
    "issue": "root-level run_pipeline.py calls query_mod.run(), gen_mod.run(), pred_mod.run() on the imported modules scripts/query_database.py, scripts/generate_candidates.py, scripts/predict_tc.py, but none of those modules define a run() function. The pipeline will raise AttributeError at step 1 (line ~308).",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Add a run() function to each of scripts/query_database.py, scripts/generate_candidates.py, scripts/predict_tc.py, or restructure the imports to call the actual top-level functions those modules provide (e.g., generate_candidates(), predict_tc())."
  },
  {
    "issue": "dft_calculator.py imports torch, torch_geometric, ase, shap at module level, but requirements.txt only lists pytest, pytest-cov, numpy, scipy, matplotlib, pandas — missing all those heavy ML/simulation dependencies. The file will crash on import.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Add torch, torch-geometric, ase, shap to requirements.txt, or make the imports lazy (inside function bodies) so the module can be imported without them present."
  },
  {
    "issue": "tests/test_api_client.py calls fetch_materials_project('mock_key', 'H3S') and asserts 'data' in result, but scripts/api_client.py's fetch_materials_project() returns a list (data.get('response', [])), not a dict with a 'data' key. The test assertion is always wrong.",
    "file": "tests/test_api_client.py",
    "severity": "medium",
    "suggestion": "Change the test assertions to match the actual return type of fetch_materials_project (a list), e.g., assert isinstance(result, list). Alternatively, fix the return type in scripts/api_client.py to return a dict with a 'data' key if that's the intended contract."
  },
  {
    "issue": "root-level run_pipeline.py is a monolithic ~10,000+ line file containing duplicate function definitions (e.g., generate_press_release, generate_slide_deck, sobol_sensitivity_analysis are defined multiple times with different bodies), two conflicting FastAPI app definitions (one with @app decorators, another standalone), and hundreds of lines of dead/commented code. It also duplicates the FastAPI app defined in app.py.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor run_pipeline.py into separate modules: keep only the orchestration logic, extract FastAPI endpoints into a dedicated api.py, remove duplicate function definitions, and delete the second FastAPI app definition. Consolidate with app.py."
  },
  {
    "issue": "README.md references files and directories that do not exist in the project: config.yaml, business_plan.md, press_release.md, output/ directory, figures/ directory, .github/workflows/ci.yml, superconductor.db, optimized_synthesis_params.json, predictions_log.json, output_ranked.py, and the 'product_closure_report' typo link to 'project_closure_report'.",
    "file": "README.md",
    "severity": "medium",
    "suggestion": "Either create the missing files/directories or update README.md to remove references to non-existent assets. At minimum, remove broken links and mark unimplemented features clearly as 'planned'."
  }
]
```

## Next actions

_(no actionable items detected — the deliverable may be analysis-only)_
