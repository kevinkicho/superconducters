# Council: directive answer

_4 drafters across 0/0 rounds — directive: "study superconducting materials (with online study if necessary) then please com…"_

> Generated by ollama_swarm preset: **council** · run e182490a-907b-439b-b0a5-2c7df9859734 · 2026-07-09T17:14:51.613Z

## Contents

- [Directive](#directive)
- [Answer to directive](#answer-to-directive)
- [Per-agent positions (latest)](#per-agent-positions-latest)
- [Round 0 — final drafts (full text)](#round-0-final-drafts-full-text)
- [Round 1 — independent first drafts (peer-hidden)](#round-1-independent-first-drafts-peer-hidden)
- [Next actions](#next-actions)

## Directive

study superconducting materials (with online study if necessary) then please come up with chemistry and physics to discover and manufacture room temperature superconducting compounds

## Answer to directive

Based on thorough analysis of the repository and all agent findings, here is the consolidated action plan:

```json
[
  {
    "description": "Consolidate and rewrite proposed_chemistry_physics.md as the single authoritative document for room-temperature superconductor chemistry and physics. Merge content from root and docs/proposed_chemistry_physics.md, restructure with sections: (a) chemical systems and design principles including Sc-induced gap unification, chemical precompression, pressure-quench metastable retention, (b) physical mechanisms covering anharmonic electron-phonon coupling, zentropy theory, flat-band moiré engineering, (c) manufacturing roadmap with scalable multi-anvil press synthesis, pressure-quench protocol (PQP), thin-film epitaxial stabilization, citing the 151 K ambient-pressure record, (d) comparative analysis table of all candidate families with Tc, pressure, feasibility, (e) cross-reference table mapping each section to computational scripts (scripts/predict_tc.py, dft_calculator.py, generate_candidates.py) and data files, (f) open challenges. Integrate all 2024-2026 breakthroughs: LaSc₂H₂₄ (271-298 K, first reproducible RTSC), zentropy theory, twisted WSe₂ moiré SC, H₃S tunneling gap (Du 2025), pressure-quenched Hg-1223 (151 K ambient-pressure record, Deng 2026), Li-Na-H clathrates (Li₂NaH₁₇, LiNa₃H₂₃), AI causal discovery for kagome superconductors, PNAS 2026 programmatic agenda.",
    "expectedFiles": ["proposed_chemistry_physics.md"]
  },
  {
    "description": "Update candidate_materials.md with new top-priority candidates: LaSc₂H₂₄ (Tc 271-298 K at 195-266 GPa, first reproducible room-temperature superconductor, hexagonal P6/mmm clathrate), Li-Na-H clathrate systems (Li₂NaH₁₇ predicted Tc ~300+ K, LiNa₃H₂₃), Mg₄Pt₃H₆ (ambient-pressure hydride superconductor), and pressure-quenched Hg-1223 (151 K ambient-pressure record holder). Include synthesis routes, precursor requirements, thermal/chemical stability data, and cross-references to proposed_chemistry_physics.md sections.",
    "expectedFiles": ["candidate_materials.md"]
  },
  {
    "description": "Update docs/online_research_summary.md with findings from targeted web searches on 2024-2026 breakthroughs: LaSc₂H₂₄ reproducibility (Song et al., arXiv:2510.01273, 2025), UH pressure-quench protocol (Deng & Chu, PNAS 2026), PNAS programmatic research agenda (Prasankumar et al., 2026), LaH₁₀ long-term stability (>5 years at 170 GPa), Mg₄Pt₃H₆ ambient-pressure hydride, AI causal discovery for CsV₃Sb₅ (Tohoku/Fujitsu 2025), zentropy theory (Liu & Shang, Supercond. Sci. Technol. 2025), H₃S tunneling gap confirmation (Du et al., Nature 2025). Remove duplicated pipeline-validation content; reorganize to clearly separate literature survey from code metrics.",
    "expectedFiles": ["docs/online_research_summary.md"]
  },
  {
    "description": "Update literature_review.md with citations for key breakthrough papers: LaSc₂H₂₄ (Song et al., arXiv:2510.01273, 2025), H₃S tunneling gap (Du et al., Nature 2025), pressure-quenched Hg-1223 at 151 K ambient-pressure record (Deng & Chu, PNAS 2026), AI causal discovery for kagome superconductor CsV₃Sb₅ (Tohoku University/Fujitsu, 2025), zentropy theory unifying BCS and DFT (Liu & Shang, Supercond. Sci. Technol. 2025), PNAS programmatic agenda for room-temperature superconductivity (Prasankumar et al., 2026). Ensure all citations include working DOIs and URLs.",
    "expectedFiles": ["literature_review.md"]
  },
  {
    "description": "Replace the stub content in docs/discovery_strategy.md with a substantive document that cross-references the root discovery_strategy.md and adds: (a) summary of the multi-criteria decision support algorithm used for candidate prioritization, (b) the pressure-quench protocol (PQP) as a viable pathway to ambient-pressure superconductivity, (c) the 'prediction challenge + engineering challenge' framework from the 2026 PNAS perspective article, (d) links to the updated proposed_chemistry_physics.md sections on manufacturing and chemical precompression.",
    "expectedFiles": ["docs/discovery_strategy.md"]
  },
  {
    "description": "Update docs/executive_summary.md to synthesize chemistry and physics into a standalone discovery-and-manufacturing roadmap. Replace the outdated LaH₁₀-centric summary with a broader view covering: ternary clathrate hydrides (LaSc₂H₂₄, Li-Na-H), cuprates (Hg-1223 PQP at 151 K ambient-pressure), nickelates (La₃Ni₂O₇ at 80 K under 14 GPa), physical mechanisms (BCS/Eliashberg, anharmonicity, zentropy, unconventional pairing, AI-guided discovery), pressure-reduction pathways (chemical precompression, PQP, thin-film strain), and updated KPI table with latest experimental benchmarks.",
    "expectedFiles": ["docs/executive_summary.md"]
  },
  {
    "description": "Fix critical runtime errors in the computational pipeline: (a) add a module-level `def run(compound: str) -> dict:` wrapper function to dft_calculator.py that parses the compound and calls `run_full_dft_calculation()`, returning standardized keys 'Tc', 'lambda', 'omega_log' — this resolves the AttributeError from run_pipeline.py calling `dft_mod.run(compound)` at 5 locations; (b) promote `_structure_to_graph(self, structure)` to a module-level `structure_to_graph(structure)` function or replace the call in `compute_ab_initio_tc()` with `MLTcPredictor()._structure_to_graph(structure)` — fixes NameError at line 663; (c) fix run_pipeline.py line 5987: change `\"fallback\": true` to `\"fallback\": True` (lowercase true is not a Python literal); (d) remove duplicate `sobol_sensitivity_analysis` definitions (4 copies), keeping only the most complete version.",
    "expectedFiles": ["dft_calculator.py", "run_pipeline.py"]
  },
  {
    "description": "Fix scripts/predict_tc.py import crash and normalize data/superconductor_database.json: (a) fix `load_database()` default path from `'superconductor_data.json'` to `os.path.join(os.path.dirname(__file__), '..', 'data', 'superconductor_database.json')` — resolves FileNotFoundError on import; (b) remove the module-level `TRAINING_DATA = load_database()` call (line 216) that overwrites the embedded list and crashes; use lazy initialization instead; (c) consolidate two conflicting `def main():` definitions (lines 325 and 781) into a single unified entry point with one argument parser; (d) normalize data/superconductor_database.json schema to consistent key names (always use 'name', 'Tc', 'lambda', 'omega_log', 'mu_star'), deduplicate compound entries (YH6 appears 8+ times with conflicting Tc values 200-227 K; Li2MgH16 appears with Tc=200-473 K), preserving the most reliable experimental value with proper reference.",
    "expectedFiles": ["scripts/predict_tc.py", "data/superconductor_database.json"]
  }
]
```

## Per-agent positions (latest)

### Agent 3

Let me verify the critical findings across the codebase before finalizing.Let me verify the remaining critical claims by checking more files.Now I have all verified findings. Here is my consolidated Agent 3 assessment:

```json
[
  {
    "issue": "run_pipeline.py calls `dft_mod.run(compound)` at 5 locations (lines 104, 359, 500, 767, 5509) but dft_calculator.py has NO module-level `run()` function — only `run_full_dft_calculation`, `run_pw`, `run_ph`, etc. This will raise AttributeError when act

### Agent 1

I need to verify the other agents' claims against actual file contents before finalizing. Let me inspect the key files.Let me verify more claims from the other agents.Now I have verified all claims against the actual files. Here are my final findings, including agreements, corrections, and additions:

```json
[
  {
    "issue": "dft_calculator.py has NO module-level `def run()` function, yet run_pipeline.py calls `dft_mod.run(compound)` at 5 locations (lines 104, 359, 500, 767, 5509). Active-lea

### Agent 2

Now I have the full transcript from Round 1. Let me verify key claims by checking actual files before finalizing.Now I have thoroughly verified every claim against the actual code. Here are my consolidated findings as Agent 2:

```json
[
  {
    "issue": "scripts/predict_tc.py crashes on import: TRAINING_DATA = load_database() at module level tries to open 'superconductor_data.json' (wrong path) instead of 'data/superconductor_database.json'. Also two conflicting def main(): definitions (lines ~

### Agent 4

I'll verify the key claims from all agents by reading the actual code files. Let me start with the most critical ones.Let me check the function signatures in run_pipeline.py and verify the claims about what_if_analysis.Now I have thoroughly verified all claims. Here is my consolidated assessment as Agent 4:

**Verified correct (from other agents):**
- `what_if_analysis` wrong keyword args (streamlit_dashboard.py:392) — CONFIRMED
- `dft_mod.run(compound)` not found in dft_calculator.py — CONFIRME

## Round 0 — final drafts (full text)

_(no final-round drafts captured)_

## Round 1 — independent first drafts (peer-hidden)

### Agent 2

Let me examine more files to understand the project structure better.Let me check a few more files to verify the issues I'm finding.Now I have enough information to compile the issues. Let me verify a couple more things.Here are my findings:

```json
[
  {
    "issue": "Monolithic ~12,000+ line file contains entire project logic, with duplicate function definitions (generate_press_release at lines 2759 and 11216, role_based_access at 3222 and 3379, sobol_sensitivity_analysis at multiple locations), making it unmaintainable and violating single-responsibility principle.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Split into separate modules: one for ML models, one for pipeline orchestration, one for FastAPI endpoints, one for document generators. Remove all duplicate definitions and keep only one canonical version per function."
  },
  {
    "issue": "streamlit_dashboard.py calls live_experimental_validation() with no arguments, but the function signature in run_pipeline.py is live_external_validation(interval_hours=24, candidate_file='candidate_materials.md', report_file='docs/experimental_feedback_loop.md'). Similarly, what_if_analysis(temperature, pressure, doping) is called with keyword args that don't match its defined signature (candidate_file, output_file, parameters). This will raise TypeError at runtime.",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Fix the function signatures in streamlit_dashboard.py to match the actual definitions in run_pipeline.py, or refactor the functions to accept the parameters the dashboard sends. Add proper argument validation."
  },
  {
    "issue": "dft_calculator.py line 663 calls structure_to_graph(structure) as a module-level function, but no such function exists at module scope. The method _structure_to_graph exists only as an instance method on the MLTcPredictor class. This will raise NameError at runtime if compute_ab_initio_tc() is called with a pinn_model argument.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Replace the call with MLTcPredictor._structure_to_graph() or promote the method to a module-level function. Add a fallback graph construction for standalone use."
  },
  {
    "issue": "predict_tc.py has conflicting module-level definitions: TRAINING_DATA is both an embedded list literal (line ~110, 20 entries) AND the result of load_database() called at module level (line ~166). The load_database() function reads from 'superconductor_data.json' which may not exist. Additionally, train_linear_regression and train_random_forest are called at module import time, causing side effects on every import.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Remove the conflicting TRAINING_DATA redefinition. Move model training out of module import into a lazy initialization or explicit train() function. Add proper error handling for missing database file."
  },
  {
    "issue": "data/superconductor_database.json has inconsistent schema: entries use different key names (some use 'name'/'Tc', others 'composition'/'tc', others 'formula'/'predicted_Tc'). Many entries lack lambda/omega_log/mu_star fields that scripts in run_pipeline.py and predict_tc.py expect. Some entries have duplicate names (e.g., YH6 appears 5+ times with different Tc values). This causes silent errors or incorrect predictions.",
    "file": "data/superconductor_database.json",
    "severity": "medium",
    "suggestion": "Normalize the schema to a single consistent format (e.g., always use 'name', 'tc', 'lambda', 'omega_log', 'mu_star'). Deduplicate entries by name, keeping the most reliable experimental value. Add a schema validation step in the pipeline."
  },
  {
    "issue": "app.py is a minimal stub (6 endpoints, in-memory storage, no persistence) despite the README documenting a full FastAPI server with authentication, rate limiting, cloud lab integration, etc. The actual FastAPI app is buried in run_pipeline.py (with full endpoints, auth, metrics) but with no documented entry point to run it via uvicorn.",
    "file": "app.py",
    "severity": "medium",
    "suggestion": "Either consolidate the FastAPI app into app.py with proper uvicorn entry point, or clearly document how to run the FastAPI server from run_pipeline.py. Add a main block to run_pipeline.py that starts uvicorn when appropriate."
  }
]
```

### Agent 1

Now let me check the actual scripts mentioned in the README:Let me check more files for concrete issues:After thorough inspection of the actual code, here are my findings as a JSON array:

```json
[
  {
    "issue": "scripts/predict_tc.py has multiple `if __name__ == '__main__'` blocks and conflicting function definitions (predict_tc is both module name and function, multiple main() functions, DATABASE global never defined), making the module crash on import. The module-level 'TRAINING_DATA = load_database()' call at line 164 tries to load 'superconductor_data.json' (wrong path) instead of 'data/superconductor_database.json', and load_database is not even fully defined yet at that point due to interleaved definitions.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Remove the broken TRAINING_DATA override at module level. Define a single canonical main() function. Remove duplicate if __name__ blocks. Use a distinct prediction function name (e.g., predict_tc_from_db) instead of overloading the module name. Remove references to undefined DATABASE global."
  },
  {
    "issue": "tests/test_predict_tc.py calls functions that do not exist in predict_tc.py: `average_valence('H3S')` expects a Dict[str,float] not a string; `screen_materials()`, `select_candidates(n=2)`, `predict_uncertainty()`, `predict_tc_with_uncertainty()` are not defined anywhere in the module. These tests will fail at runtime with AttributeError.",
    "file": "tests/test_predict_tc.py",
    "severity": "high",
    "suggestion": "Rewrite tests to use only functions that actually exist in predict_tc.py (e.g., `extract_features`, `mcmillan_tc`, `eliashberg_tc`, `predict_tc_rf`). Remove calls to non-existent functions or implement them if intended."
  },
  {
    "issue": "run_pipeline.py is ~10,000+ lines with severe code duplication: `sobol_sensitivity_analysis` defined 3 times, `reproducibility_check` defined multiple times, `generate_candidates`, `generate_slide_deck`, `generate_press_release`, `generate_weekly_newsletter`, `role_based_access` all defined twice or more. The file also defines multiple FastAPI app objects (app, health_app, plus another in-line). Imports are scattered throughout the file body, not at the top.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor into separate modules (e.g., sensitivity.py, document_gen.py, api.py, pipeline_utils.py). Remove all duplicate function definitions. Consolidate FastAPI apps into a single app. Move all imports to the top of the file."
  },
  {
    "issue": "README.md claims 'python scripts/run_pipeline.py' runs the pipeline, but run_pipeline.py has no top-level `if __name__ == '__main__'` entry point for the pipeline; the only `if __name__` block at the end handles --watch/--validate/--simulate flags, not the default pipeline described in the README. The 'Quickstart Tutorial' in README also references 'python run_pipeline.py --candidate \"YBa2Cu3O7\"' but run_pipeline.py does not accept a --candidate argument.",
    "file": "README.md",
    "severity": "medium",
    "suggestion": "Add a default `if __name__ == '__main__'` block that runs the pipeline as described, or update README to match actual CLI usage. Add --candidate argument or update the tutorial."
  },
  {
    "issue": "dft_calculator.py's `run_full_dft_calculation` calls `os.chdir(workdir)` but never changes back to the original directory, leaving the process in an unpredictable working directory. This can cause subsequent file operations to fail silently.",
    "file": "dft_calculator.py",
    "severity": "medium",
    "suggestion": "Wrap the chdir in a try/finally block that restores the original working directory, or use absolute paths instead of changing directory."
  },
  {
    "issue": "predicted_Tc values in data/superconductor_database.json for hydride entries like 'Li2MgH16' are listed as 260 K (entry 48) and 300 K (entry 112) and 473 K (entry 184) for the same compound, with conflicting lambda values (2.8 vs 2.6 vs 2.2). Multiple duplicate entries for YH6 (entries 51, 197, 199, 209, 228) have different Tc values (200, 220, 224) and different pressures. This inconsistency will corrupt ML model training.",
    "file": "data/superconductor_database.json",
    "severity": "high",
    "suggestion": "Deduplicate entries by merging into a single record per compound with a primary Tc value and references. Use a unique compound key (e.g., 'name + pressure') to avoid ambiguity, and keep only the most reliable experimental value."
  }
]
```

### Agent 4

Let me examine more files to verify findings.Based on extensive code review, here are the critical issues found in the project:

```json
[
  {
    "issue": "streamlit_dashboard.py calls what_if_analysis with wrong keyword arguments (temperature=, pressure=, doping=) that do not match the function's actual signature which expects (candidate_file, output_file, parameters). This will raise a TypeError at runtime when the 'Run What-If' button is clicked.",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Change the call to what_if_analysis(parameters={'pressure (GPa)': [pressure], 'doping level': [doping/100], 'temperature (K)': [temperature]}) or refactor the function to accept simple scalar arguments."
  },
  {
    "issue": "run_pipeline.py defines sobol_sensitivity_analysis 4 separate times (lines ~9903, ~10104, ~10502, ~11012) with different signatures (some return None, some return dict). Later definitions silently overwrite earlier ones, creating unpredictable behavior for any caller.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Remove duplicate definitions. Keep the single most complete version (line ~11012 with 9 parameters and second-order indices) and delete all earlier copies. Consolidate into a single function."
  },
  {
    "issue": "streamlit_dashboard.py has an infinite `while True` loop containing `st.rerun()` at module level inside `main()` (lines ~362-370). Since the `else: break` only exits when auto_refresh is False, the loop causes continuous re-rendering with no delay control, consuming 100% CPU and making the dashboard unresponsive.",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Replace the `while True` loop with Streamlit's native `st.empty()` + `time.sleep(refresh_interval)` pattern outside the loop, or use `st.experimental_autorefresh()` instead of manual looping."
  },
  {
    "issue": "Importing streamlit_dashboard.py triggers massive side effects from run_pipeline.py (15,000+ lines of module-level code including FastAPI app creation, Prometheus metrics registration, global variable initialization, and multiple `if __name__` blocks that execute). This makes the import slow (~seconds) and can cause crashes due to missing optional dependencies (GPy, simpy, SALib, etc.).",
    "file": "run_pipeline.py",
    "severity": "medium",
    "suggestion": "Refactor run_pipeline.py into smaller modules (e.g., pipeline_core.py, sensitivity.py, optimization.py, reports.py) and move all non-essential imports inside the functions that need them. Remove or guard top-level code under `if __name__` checks."
  },
  {
    "issue": "dft_calculator.py has a module-level `_dft_cache = {}` and the `__main__` block at the bottom calls `run_full_dft_calculation()` which attempts to execute Quantum ESPRESSO binaries (pw.x, ph.x, etc.) via subprocess without checking if they exist, causing a FileNotFoundError crash when run as `python dft_calculator.py`.",
    "file": "dft_calculator.py",
    "severity": "medium",
    "suggestion": "Add a check for QE binary availability before attempting subprocess calls. Wrap the `__main__` example in a try/except. Alternatively, remove the executable `__main__` block and provide a pytest-based test instead."
  },
  {
    "issue": "app.py contains a `/predict` endpoint with a hardcoded dummy model (`predicted_tc = min(300, 20 + 10 * h_count)`) that simply counts hydrogen atoms and does not use any actual Tc prediction model. This makes the API endpoint misleading and useless for real candidate evaluation.",
    "file": "app.py",
    "severity": "medium",
    "suggestion": "Either remove the `/predict` endpoint or wire it to the actual predict_tc module from scripts/predict_tc.py with proper error handling and model loading."
  }
]
```

### Agent 3

Let me verify some of the issues I've identified.Based on my thorough review of the codebase, here are the concrete issues I've identified:

```json
[
  {
    "issue": "scripts/predict_tc.py crashes on import because `load_database()` (called at module level on line 300) tries to open `superconductor_data.json` which does not exist in the project. The actual database is at `data/superconductor_database.json`. This causes an immediate FileNotFoundError on any `import predict_tc`, breaking the entire pipeline.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Change the `load_database()` default path to `os.path.join(os.path.dirname(__file__), '..', 'data', 'superconductor_database.json')` or wrap the module-level call in a try/except with a fallback to the embedded TRAINING_DATA list."
  },
  {
    "issue": "scripts/predict_tc.py has two `def main():` definitions (line 325 and line 781). The second definition silently overwrites the first, making the first main()'s argument parser (formulas, --database, --train, --screen) completely dead code. The second main() expects different arguments (--model, --candidates, --output). Any caller relying on the first signature will break.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Consolidate the two `main()` functions into one with a unified argument parser that covers all use cases, or rename them to distinct entry points (e.g., `main_bcs()` and `main_ml()`)."
  },
  {
    "issue": "run_pipeline.py calls `dft_mod.run(compound)` at lines 104, 359, 500, 767, 5509 but `dft_calculator.py` has no `run()` function defined. The module defines `run_full_dft_calculation()`, `run_pw()`, `run_ph()`, etc., but no generic `run()` entry point. This will raise an AttributeError at runtime whenever the active-learning loop, multi-fidelity optimization, or candidate enrichment paths execute.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Add a `def run(compound: str) -> dict:` wrapper function at the end of `dft_calculator.py` that parses the compound, generates a structure, and calls `run_full_dft_calculation()`, returning a standardised dict with at least `{'Tc': ..., 'lambda': ..., 'omega_log': ...}`."
  },
  {
    "issue": "`run_pipeline.py` line 5987 contains `\"fallback\": true` (lowercase `true`). In Python, `true` is not a defined name and will raise a `NameError`. This occurs in the `cloud_lab_fallback_with_comparison()` function.",
    "file": "run_pipeline.py",
    "severity": "medium",
    "suggestion": "Replace `true` with `True` (uppercase) to use the Python boolean literal."
  },
  {
    "issue": "`train_gp_model()` in `scripts/predict_tc.py` (line 1463) calls `load_dft_features()` which is not defined anywhere in the module, nor imported. This will raise a `NameError` when the GP training path is executed.",
    "file": "scripts/predict_tc.py",
    "severity": "medium",
    "suggestion": "Define a `load_dft_features()` function that reads from `data/experimental_results.json`, or remove the call and inline the DFT feature loading logic."
  },
  {
    "issue": "The README claims the pipeline can be run with `python run_pipeline.py --candidate \"YBa2Cu3O7\"`, but `run_pipeline.py`'s argument parser (line 357) has no `--candidate` or `--output-dir` flags. The user manual describes a `--candidate` CLI argument that does not exist in the code.",
    "file": "run_pipeline.py",
    "severity": "low",
    "suggestion": "Add the missing `--candidate` and `--output-dir` arguments to the argument parser, or update the README to reflect the actual CLI interface."
  }
]
```

## Next actions

_(no actionable items detected — the deliverable may be analysis-only)_
