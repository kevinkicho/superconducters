# Council: directive answer

_3 drafters across 0/0 rounds — directive: "study superconducting materials (with online study if necessary) then please com…"_

> Generated by ollama_swarm preset: **council** · run d21cf8b3-3f41-4241-910e-29a2bfe52214 · 2026-07-09T22:15:27.426Z

## Contents

- [Directive](#directive)
- [Answer to directive](#answer-to-directive)
- [Per-agent positions (latest)](#per-agent-positions-latest)
- [Round 0 — final drafts (full text)](#round-0-final-drafts-full-text)
- [Round 1 — independent first drafts (peer-hidden)](#round-1-independent-first-drafts-peer-hidden)
- [Next actions](#next-actions)

## Directive

study superconducting materials (with online study if necessary) then please come up with chemistry and physics to discover and manufacture room temperature superconducting compounds

## Answer to directive

I've analyzed all 3 agents' findings, verified them against the actual files, and synthesized the following merged action plan. The highest-priority items directly serve the user directive (study superconductors → propose chemistry/physics for discovery & manufacturing).

---

## Merged Action Plan

### 1. Add 2025–2026 Breakthrough Sections to `proposed_chemistry_physics.md`

**Why**: The root-level `proposed_chemistry_physics.md` is the authoritative chemistry/physics document for the directive, but it lacks the 7 major breakthroughs that the `docs/` stub claims exist. This is the single most critical gap.

**What**: Add dedicated sections for:
- **LaSc₂H₂₄** — first reproducible room-temperature superconductor (Tc 271–298 K at ~260 GPa), hexagonal clathrate structure, Sc-induced gap unification mechanism
- **Zentropy theory** — first-principles Tc prediction bridging BCS and DFT, validated on 18 elements + YBa₂Cu₃O₇
- **Pressure-quench protocol (PQP)** — Hg-1223 ambient-pressure Tc record of 151 K (Deng & Chu, 2026)
- **H₃S tunneling gap anomaly** — first direct gap measurement in a hydride (~60 meV), isotope-effect confirmation
- **Deep-learning discovery of 144 ternary hydrides** (Wang et al., arXiv:2502.16558)
- **Nickelate advances** — La₃Ni₂O₇ single crystals Tc up to 96 K, ambient-pressure thin films >40 K
- **AI causal discovery** — Fujitsu/Tohoku causal AI on CsV₃Sb₅ kagome ARPES data
- **Updated manufacturing roadmap** — PQP, chemical precompression, Mg₄Pt₃H₆ ambient-stable hydrides
- **Prioritized candidate table** with latest Tc/pressure data

**Files**: `proposed_chemistry_physics.md`

---

### 2. Fix `docs/proposed_chemistry_physics.md` Stub to Match Root File

**Why**: The stub claims 7 breakthroughs are "documented in full" in the root file, which is currently false. After todo #1, the stub must be verified against actual section headings.

**What**: Update the cross-reference table to accurately list all section headings present in the root-level file. Remove any claims about sections that don't exist.

**Files**: `docs/proposed_chemistry_physics.md`

---

### 3. Remove Fabricated References from `candidate_materials.md`

**Why**: ~15 predicted compounds (CsH9, RbH8, BaH12, Li2CaH18, Na2MgH16, SrH10, K2CaH20, Rb2CaH20, Cs2MgH18) have references like "Smith et al., Phys. Rev. B 105, 2022" that are not real papers. This undermines scientific credibility.

**What**: Remove all entries with unverifiable references. Keep only experimentally confirmed or rigorously predicted compounds from verifiable peer-reviewed literature. Add a clear disclaimer for predicted-only entries.

**Files**: `candidate_materials.md`

---

### 4. Deduplicate `data/superconductor_database.json`

**Why**: YH6 appears 5+ times with conflicting Tc values (200–227 K), LaH10 3+ times, C-S-H 4+ times, etc. This corrupts ML training by giving conflicting targets for the same material.

**What**: Keep one entry per unique material with the most reliable Tc value and citation. Add a `duplicate_of` field for cross-references. Use a unique `id` field. Remove placeholder entries (PrH9=200K, CeH9=190K, etc.) not from real literature.

**Files**: `data/superconductor_database.json`

---

### 5. Fix `tests/test_predict_tc.py` to Match Actual API

**Why**: Tests call non-existent functions (`screen_materials`, `select_candidates`, `predict_uncertainty`, `allen_dynes_tc`, `predict_tc_with_uncertainty`, `generate_candidates`) and pass wrong argument types (string instead of dict to `average_valence`/`average_debye`). All tests will fail.

**What**: Rewrite tests to use actual API: `mcmillan_tc(formula)`, `eliashberg_tc(formula)`, `predict_tc_rf(formula)`, `average_valence(dict)`, `average_debye(dict)`. Remove tests for non-existent functions.

**Files**: `tests/test_predict_tc.py`

---

### 6. Consolidate `scripts/predict_tc.py` — Fix Multiple `predict_tc()` Definitions and `screen_candidates()` Bug

**Why**: `predict_tc()` is defined 3 times (lines 221, 869, 1317) with different signatures; the last (no-arg) overwrites earlier versions, breaking `generate_candidates.py`. Also, `screen_candidates()` calls `average_valence(formula)` with a string but the function expects `Dict[str, float]`.

**What**: 
- Consolidate to a single `predict_tc(formula, pressure)` function
- Rename database-printing version to `predict_tc_from_db()`
- Fix `screen_candidates()` to call `parse_formula()` first
- Remove placeholder `HYDRIDE_DATABASE` values or replace with real literature values

**Files**: `scripts/predict_tc.py`

---

### 7. Replace Dummy Tc Model in `app.py` with Real ML Models

**Why**: `app.py` uses `predicted_tc = min(300, 20 + 10 * h_count)` instead of importing trained models from `scripts/predict_tc.py`. The `/batch_predict` endpoint returns placeholder values.

**What**: Import and use `predict_tc_rf()` or `eliashberg_tc()` from `scripts/predict_tc.py` in the FastAPI endpoints. Add proper error handling for unknown compounds.

**Files**: `app.py`

---

### 8. Fix `streamlit_dashboard.py` Import Side Effects

**Why**: `streamlit_dashboard.py` imports from `run_pipeline` (line 10), which triggers all side effects at the bottom of `run_pipeline.py` (health server, press releases, undefined function calls), crashing the dashboard on import.

**What**: Extract shared functions (`live_external_validation`, `what_if_analysis`, etc.) into a separate module (e.g., `pipeline_shared.py`) that both `run_pipeline.py` and `streamlit_dashboard.py` can import without triggering side effects. Guard the `if __name__ == '__main__'` block in `run_pipeline.py`.

**Files**: `streamlit_dashboard.py`, `run_pipeline.py` (or new `pipeline_shared.py`)

---

```json
[
  {
    "description": "Add 2025-2026 breakthrough sections (LaSc₂H₂₄, Zentropy, PQP, H₃S gap, deep-learning ternary hydrides, nickelate advances, AI causal discovery, updated manufacturing roadmap, prioritized candidate table) to root-level proposed_chemistry_physics.md",
    "expectedFiles": ["proposed_chemistry_physics.md"]
  },
  {
    "description": "Update docs/proposed_chemistry_physics.md stub to accurately cross-reference actual section headings in the root-level file after breakthroughs are added",
    "expectedFiles": ["docs/proposed_chemistry_physics.md"]
  },
  {
    "description": "Remove fabricated references from candidate_materials.md for predicted compounds (CsH9, RbH8, BaH12, Li2CaH18, Na2MgH16, SrH10, K2CaH20, Rb2CaH20, Cs2MgH18) and replace with only verifiable peer-reviewed entries",
    "expectedFiles": ["candidate_materials.md"]
  },
  {
    "description": "Deduplicate data/superconductor_database.json — keep one entry per unique material with most reliable Tc, remove placeholder entries, add duplicate_of field",
    "expectedFiles": ["data/superconductor_database.json"]
  },
  {
    "description": "Fix tests/test_predict_tc.py to match actual API of scripts/predict_tc.py (use mcmillan_tc(formula), eliashberg_tc(formula), predict_tc_rf(formula), average_valence(dict), average_debye(dict))",
    "expectedFiles": ["tests/test_predict_tc.py"]
  },
  {
    "description": "Consolidate multiple predict_tc() definitions in scripts/predict_tc.py into one, fix screen_candidates() string-vs-dict bug, remove placeholder HYDRIDE_DATABASE values",
    "expectedFiles": ["scripts/predict_tc.py"]
  },
  {
    "description": "Replace dummy Tc formula in app.py with real ML model imports from scripts/predict_tc.py (RandomForest/GP) for /predict-tc and /batch_predict endpoints",
    "expectedFiles": ["app.py"]
  },
  {
    "description": "Extract shared functions from run_pipeline.py into pipeline_shared.py to fix streamlit_dashboard.py import side effects (health server, undefined function calls)",
    "expectedFiles": ["streamlit_dashboard.py", "run_pipeline.py"]
  }
]
```

## Per-agent positions (latest)

### Agent 3

Here are my verified findings as a JSON array:
```json
[
  {
    "issue": "docs/proposed_chemistry_physics.md stub claims 7 breakthroughs (LaSc₂H₂₄, Zentropy, Twisted WSe₂, H₃S tunneling gap, PQP Hg-1223, Li-Na-H clathrates, AI causal discovery) are 'documented in full' in the root-level proposed_chemistry_physics.md, but the root-level file has NO dedicated sections on any of these topics. The stub is misleading and factually incorrect.",
    "file": "docs/proposed_chemistry_physics.md",
    "s

### Agent 2

Now I have thoroughly verified all claims. Here are my findings as Agent 2:

```json
[
  {
    "issue": "Agent 3 incorrectly claimed torch-geometric and shap are missing from requirements.txt. Both ARE present (torch-geometric>=2.3, shap>=0.41). This is a false positive — the dependencies are correctly listed.",
    "file": "requirements.txt",
    "severity": "low",
    "suggestion": "No action needed — dependencies are correctly listed."
  },
  {
    "issue": "tests/test_predict_tc.py calls fun

### Agent 1

Now I have thoroughly verified all claims. Here are my findings:

```json
[
  {
    "issue": "tests/test_predict_tc.py calls functions that do not exist in scripts/predict_tc.py: ptc.screen_materials(), ptc.select_candidates(n=2), ptc.predict_uncertainty('H3S', model=model), ptc.generate_candidates(n=2), ptc.predict_tc_with_uncertainty('H3S', pressure=155), ptc.allen_dynes_tc(2.0, 1000, mu_star=0.1). Also calls ptc.mcmillan_tc(2.0, 1000, mu_star=0.1) with 3 args but actual mcmillan_tc(formula: s

## Round 0 — final drafts (full text)

_(no final-round drafts captured)_

## Round 1 — independent first drafts (peer-hidden)

### Agent 2

Now I have a thorough understanding of the codebase. Here are my findings:

```json
[
  {
    "issue": "run_pipeline.py is a monolithic ~10,000+ line file containing ALL project logic, with massive code duplication (sobol_sensitivity_analysis defined 3+ times, digital_twin_simulation defined twice, generate_candidates defined multiple times), conflicting imports, multiple main() functions, and dead code. This makes the file unmaintainable and impossible to test.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor run_pipeline.py into separate modules by concern (e.g., ml_models.py, synthesis.py, optimization.py, reporting.py). Remove all duplicate function definitions. Keep only the pipeline orchestration logic in run_pipeline.py and import from the new modules."
  },
  {
    "issue": "tests/test_predict_tc.py calls functions that do not exist in scripts/predict_tc.py: ptc.screen_materials(), ptc.select_candidates(), ptc.predict_uncertainty(), ptc.generate_candidates(), ptc.predict_tc_with_uncertainty(), ptc.allen_dynes_tc(), ptc.mcmillan_tc() (with different signature), ptc.average_valence() (with single string argument), ptc.average_debye() (with single string argument). All these tests will fail with AttributeError.",
    "file": "tests/test_predict_tc.py",
    "severity": "high",
    "suggestion": "Either implement the missing functions in scripts/predict_tc.py with the expected signatures, or rewrite the tests to match the actual API of the module (e.g., use ptc.mcmillan_tc(formula) which takes a formula string, ptc.eliashberg_tc(formula), ptc.predict_tc_rf(formula), etc.)."
  },
  {
    "issue": "scripts/predict_tc.py has a top-level function named predict_tc() that reads the entire database and prints predictions, but scripts/generate_candidates.py imports from scripts.predict_tc import predict_tc expecting a function that takes a formula string and returns a float Tc. The actual predict_tc() function requires a 'name' field in the database and raises ValueError if the material is not found, making it unusable for novel candidate prediction.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Rename the database-printing function to predict_from_database() or similar. Create a new predict_tc(formula) function that uses the trained RandomForest model (predict_tc_rf) or the Eliashberg model (eliashberg_tc) to return a single Tc value for any formula string. Update generate_candidates.py to use the correct function."
  },
  {
    "issue": "app.py (FastAPI server) uses a dummy Tc prediction model that just counts hydrogen atoms (predicted_tc = min(300, 20 + 10 * h_count)). It does not import or use any of the trained ML models from scripts/predict_tc.py (RandomForest, GaussianProcess, PINN, GNN). The /batch_predict endpoint returns placeholder predictions with no real physics.",
    "file": "app.py",
    "severity": "medium",
    "suggestion": "Import and use the trained models from scripts/predict_tc.py in the FastAPI endpoints. For /predict-tc, use the RandomForest or GP model. For /batch_predict, use the same model in batch mode. Add proper error handling for unknown compounds."
  },
  {
    "issue": "dft_calculator.py references Quantum ESPRESSO binaries (pw.x, ph.x, q2r.x, matdyn.x, lambda.x) via hardcoded subprocess calls. These binaries are not installed in the project environment (not in requirements.txt, not in the Dockerfile). The compute_eph_coupling function will fail with FileNotFoundError on any system without QE installed.",
    "file": "dft_calculator.py",
    "severity": "medium",
    "suggestion": "Add a check at the top of dft_calculator.py that verifies QE binaries are available and raises a clear ImportError or RuntimeError with installation instructions if not. Alternatively, provide a mock/simulation fallback mode for systems without QE. Add QE installation to the Dockerfile and environment.yml."
  },
  {
    "issue": "scripts/predict_tc.py has multiple main() entry points (main(), main_screening(), main_pinn(), main_gnn()) and multiple if __name__ == '__main__' blocks that conflict. The file also defines a class ModelEnsemble that references a 'transformer_model' which is never defined anywhere in the project. The HYDRIDE_DATABASE at module level contains placeholder Tc values (e.g., PrH9=200K, CeH9=190K, NdH9=195K) that are not from real literature.",
    "file": "scripts/predict_tc.py",
    "severity": "medium",
    "suggestion": "Consolidate to a single main() function with subcommands (argparse subparsers). Remove the transformer_model reference or implement it. Replace placeholder hydride Tc values with actual literature values or mark them clearly as estimates. Remove dead code like the unused ModelEnsemble class."
  }
]
```

### Agent 3

Here are my findings as a structured JSON array:

```json
[
  {
    "issue": "run_pipeline.py is a monolithic ~12,000+ line file containing hundreds of function definitions, multiple FastAPI apps, Streamlit dashboards, and all pipeline logic in a single file. This makes the codebase unmaintainable, untestable, and impossible to reason about. The file defines ~200+ functions including many duplicates.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor into separate modules: one for the FastAPI app (app.py already exists but is empty), one for pipeline orchestration, one for ML models, one for utility functions, one for document generation, etc. Each module should have a single responsibility."
  },
  {
    "issue": "streamlit_dashboard.py imports from run_pipeline (line 10: `from run_pipeline import live_external_validation, what_if_analysis, generate_press_release, generate_monthly_report`). This triggers all side effects at the bottom of run_pipeline.py, including starting a health server, generating press releases, running Sobol analysis, and calling undefined functions like `multi_objective_optimization()` and `virtual_lab_simulation()`. This will crash the dashboard on import.",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Extract the shared functions (live_external_validation, what_if_analysis, etc.) into a separate shared module (e.g., pipeline_shared.py) that both run_pipeline.py and streamlit_dashboard.py can import without triggering side effects. Remove the `if __name__ == '__main__'` block from run_pipeline.py or guard it properly."
  },
  {
    "issue": "scripts/predict_tc.py has at least 3 separate `if __name__ == '__main__'` blocks (around lines 1-100, the `main_screening` section, and the `main_gnn` section). Only the last one executes, making the other two dead code. Additionally, the module defines a function `predict_tc()` at module level which shadows the module name itself, making `from predict_tc import predict_tc` ambiguous and error-prone.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Consolidate into a single `if __name__ == '__main__'` block with argparse subcommands. Rename the `predict_tc()` function to something like `predict_tc_from_db()` to avoid shadowing the module name. Remove duplicate/redundant code."
  },
  {
    "issue": "dft_calculator.py imports `torch_geometric` and `shap` which are NOT listed in requirements.txt. The file also references environment variables `QE_PW`, `QE_PH`, `QE_Q2R`, `QE_MATDYN`, `QE_LAMBDA` for Quantum ESPRESSO binaries that are never installed or configured. The `run_full_dft_calculation()` function will fail with FileNotFoundError on any system without QE installed.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Add torch-geometric and shap to requirements.txt. Add a configuration check at module load time that verifies QE binaries exist and provides a clear error message. Consider adding a mock/simulation mode for systems without QE."
  },
  {
    "issue": "run_pipeline.py defines many functions multiple times (e.g., `generate_press_release` at lines 2759 and 11216; `sobol_sensitivity_analysis` at lines 9903, 10104, 10502, and 11012; `reproducibility_check` at lines 10041 and 11584; `generate_slide_deck` at lines 1619 and 3131; `generate_research_paper` at lines 2062 and 4147; `generate_discovery_announcement` at lines 8017 and 8357; `validate_with_supercon` at lines 4783 and 6482; `integrate_real_cloud_lab` at lines 6522 and 8101; `study_superconductors` at lines 8197 and 4548; `role_based_access` at lines 3222 and 3379; `digital_twin_simulation` at lines 441 and 9863; `check_regulatory_compliance` at lines 2655 and 3153; `generate_patent_landscape` at lines 3142 and 1253; `generate_experimental_proposal_report` at lines 3098 and 797; `generate_public_outreach_summary` at lines 3109 and 4672; `generate_comprehensive_final_report` at lines 3120 and 3098; `submit_to_arxiv` at lines 6651 and 6981; `real_time_collaboration` at lines 5466 and 5935; `cloud_lab_integration` at lines 3398 and 5644; `generate_discovery_report` at lines 3925 and 7973; `generate_materials_physics_report` at line 8065). The last definition wins, making all earlier definitions dead code.",
    "file": "run_pipeline.py",
    "severity": "medium",
    "suggestion": "Deduplicate all function definitions. Keep only the most complete/up-to-date version of each function. Use a linter (e.g., flake8, pylint) with duplicate-code detection to prevent this in the future."
  },
  {
    "issue": "The `__main__` block at the bottom of run_pipeline.py (around line 11500) calls `multi_objective_optimization()`, `virtual_lab_simulation()`, and `reproducibility_check()` which are defined later in the file (lines 11518, 11552, 11584) — these are only defined AFTER the `if __name__ == '__main__'` block, so they are NOT in scope when the block executes. This will raise NameError at runtime.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Move the `if __name__ == '__main__'` block to the very end of the file, after all function definitions. Or better, use a proper entry-point script (scripts/run_pipeline.py) that imports from the main module."
  }
]
```

### Agent 1

Now I have a thorough understanding of the codebase. Here are my findings:

```json
[
  {
    "issue": "run_pipeline.py is a monolithic ~10,000+ line file with massive function duplication — functions like sobol_sensitivity_analysis, generate_press_release, generate_monthly_report, generate_candidates, generate_patent_draft, generate_discovery_report, generate_discovery_announcement, generate_technology_transfer_plan, generate_regulatory_submission, check_regulatory_compliance, generate_slide_deck, generate_experimental_proposal_report, generate_public_outreach_summary, generate_comprehensive_final_report, generate_patent_landscape, perform_global_sensitivity_analysis, integrate_arxiv_scraper, generate_weekly_newsletter, export_candidate_data, send_slack_alert, llm_review_proposed_chemistry, generate_model_validation_report, compute_mrl, generate_reproducibility_package, generate_discovery_report, generate_discovery_announcement, generate_materials_physics_report, integrate_real_cloud_lab, study_superconductors, project_trl_assessment, generate_project_closure_report, project_retrospective, generate_discovery_announcement, controversy_analysis, data_consistency_validation, watch_mode, high_throughput_screening, validate_model, retrain_active_learning, VirtualLabSimulator, assimilate_experimental_data, run_closed_loop_simulation, watch_and_retrain, MultiFidelityGP, SynthesisEnv, train_rl_agent, comprehensive_validation, data_assimilation_loop, CloudLabClient, CrystalStructurePredictor, MultiFidelityGP, run_validation, integrate_manufacturing_cost, run_benchmark, DataVersioning, self_optimize_pipeline, setup_logging, fetch_and_merge_materials_project_data, predict_tc_batch, generate_patent_draft, automated_document_sync, supercon_nims_integration, digital_twin_simulation, self_healing_mechanism, sobol_sensitivity_analysis, decision_support_system, reproducibility_check, sobol_sensitivity_analysis, factory_simulation, uncertainty_quantification, pipeline_benchmark, SynchrotronClient, ensemble_kalman_filter, automated_hypothesis_generation, sobol_sensitivity_analysis, patent_paper_generation, pdf_report_generation, high_throughput_screening_ternary_hydrides, generate_synthesis_protocol, data_quality_checks, expected_calibration_error, run_high_throughput_screening, sobol_sensitivity_analysis, validate_unified_model, generate_press_release, generate_monthly_report, generate_funding_proposal, generate_stakeholder_newsletter, run_sobol_analysis, decision_support, start_health_server, data_lineage_tracking, continuous_model_retraining, multi_objective_optimization, virtual_lab_simulation, reproducibility_check, integrate_arxiv_and_dft are each defined multiple times (2-5+ times each). In Python, the last definition silently overwrites earlier ones, meaning most of these functions are dead code that never executes.",
    "file": "run_pipeline.py",
    "severity": "high",
    "suggestion": "Refactor run_pipeline.py into separate modules (e.g., pipeline_core.py, sensitivity.py, reporting.py, cloud_lab.py, simulation.py, etc.) with no duplicate function definitions. Each function should be defined exactly once."
  },
  {
    "issue": "streamlit_dashboard.py imports live_external_validation, what_if_analysis, generate_press_release, generate_monthly_report from run_pipeline, but these functions are defined multiple times in run_pipeline.py. The last definition of each may not match what the dashboard expects (e.g., the last generate_press_release writes to 'press_release.md' while the dashboard expects a return value).",
    "file": "streamlit_dashboard.py",
    "severity": "high",
    "suggestion": "Ensure each imported function is defined exactly once in run_pipeline.py with a consistent signature. Better yet, move shared functions to a separate module (e.g., pipeline_utils.py) and import from there."
  },
  {
    "issue": "app.py is a stub with in-memory storage, no persistence, and a dummy Tc prediction formula (20 + 10 * h_count). It does not use any of the sophisticated models in scripts/predict_tc.py (Random Forest, GNN, PINN, GP). The /batch_predict endpoint returns placeholder values with no real model inference.",
    "file": "app.py",
    "severity": "high",
    "suggestion": "Integrate app.py with the actual ML models from scripts/predict_tc.py. Add database persistence (e.g., SQLite or the existing JSON files). Replace the dummy Tc formula with calls to the trained Random Forest or GP model."
  },
  {
    "issue": "scripts/predict_tc.py has multiple main() entry points (main(), main_screening(), main_gnn(), main_pinn()) and multiple if __name__ == '__main__' blocks. Only the last __main__ block executes. The predict_tc() function (line ~200) tries to load a database at relative path '../data/superconductor_database.json' and expects entries to have 'lambda', 'omega_log', 'mu_star' keys, but many entries in the actual database lack these keys (e.g., C-S-H, Nd0.8Sr0.2NiO2, FeSe, etc.), causing KeyError or ValueError at runtime.",
    "file": "scripts/predict_tc.py",
    "severity": "high",
    "suggestion": "Consolidate to a single main() entry point. Fix predict_tc() to handle missing keys gracefully (e.g., using .get() with defaults). Use os.path.join(os.path.dirname(__file__), '..', 'data', 'superconductor_database.json') consistently."
  },
  {
    "issue": "dft_calculator.py references Quantum ESPRESSO binaries (pw.x, ph.x, q2r.x, matdyn.x, lambda.x) via subprocess calls in compute_eph_coupling() and run_full_dft_calculation(). These binaries are not installed in the environment (not listed in requirements.txt) and are not available on PATH. The functions will raise FileNotFoundError at runtime. The compute_dos_at_fermi() function also tries to run pw.x via ASE's Espresso calculator, which will fail without QE installed.",
    "file": "dft_calculator.py",
    "severity": "high",
    "suggestion": "Add try/except wrappers around all subprocess calls with informative error messages. Document that Quantum ESPRESSO must be installed separately. Consider adding a mock/simulation fallback for environments without QE, or move QE-dependent functions to a separate optional module."
  },
  {
    "issue": "scripts/predict_tc.py defines a global _current_model variable and set_model() function but never uses them — the predict_tc() function always uses the database-based Allen-Dynes formula, not the selected model. The GNN training code (train_gnn, CrystalGNN) and PINN code (BCSPINN) are defined but never called from the main pipeline. The active_learning_loop_gnn() function requires a pre-trained GNN model but no training pipeline connects the database to GNN training.",
    "file": "scripts/predict_tc.py",
    "severity": "medium",
    "suggestion": "Either remove the unused model-switching infrastructure or wire it up so that set_model() actually changes which predict function is called. Connect the GNN/PINN training to the main pipeline so models are actually trained and used."
  }
]
```

## Next actions

**MEDIUM priority:**
- Consolidate to a single `predict_tc(formula, pressure)` function _(from: 6. Consolidate `scripts/predict_tc.py` — Fix Multiple `predict_tc()` Definitions and `screen_candidates()` Bug)_
- Rename database-printing version to `predict_tc_from_db()` _(from: 6. Consolidate `scripts/predict_tc.py` — Fix Multiple `predict_tc()` Definitions and `screen_candidates()` Bug)_
- Fix `screen_candidates()` to call `parse_formula()` first _(from: 6. Consolidate `scripts/predict_tc.py` — Fix Multiple `predict_tc()` Definitions and `screen_candidates()` Bug)_
- Remove placeholder `HYDRIDE_DATABASE` values or replace with real literature values _(from: 6. Consolidate `scripts/predict_tc.py` — Fix Multiple `predict_tc()` Definitions and `screen_candidates()` Bug)_
- "suggestion": "Refactor into separate modules: one for the FastAPI app (app.py already exists but is empty), one for pipeline orchestration, one for ML models, one for utility functions, one for document generation, etc. Each module should have a single responsibility." _(from: Agent 3)_
- "suggestion": "Refactor run_pipeline.py into separate modules (e.g., pipeline_core.py, sensitivity.py, reporting.py, cloud_lab.py, simulation.py, etc.) with no duplicate function definitions. Each function should be defined exactly once." _(from: Agent 1)_
